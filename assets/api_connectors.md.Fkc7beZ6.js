import{_ as t,o as s,c as a,a2 as i}from"./chunks/framework.D1JEzK2q.js";const k=JSON.parse('{"title":"Connectors API","description":"","frontmatter":{},"headers":[],"relativePath":"api/connectors.md","filePath":"api/connectors.md"}'),n={name:"api/connectors.md"};function o(r,e,d,c,h,l){return s(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="connectors-api" tabindex="-1">Connectors API <a class="header-anchor" href="#connectors-api" aria-label="Permalink to &quot;Connectors API {#connectors-api}&quot;">​</a></h1><p>A <strong>connector</strong> is an MCP server the gateway injects into a Claude Code session&#39;s <code>mcp-config.json</code>. The gateway owns three things and nothing else: the connector definition, the per-connector secret, and the per-agent enablement. At spawn, an enabled + connected connector is resolved into an <code>mcpServers</code> entry and Claude Code then talks to the real MCP server directly.</p><p>Every connector lives in <code>gateway.customConnectors</code> in <code>config.json</code> — admin-trusted raw <code>mcpServers</code> JSON, <strong>not</strong> code-reviewed. Connectors differ along exactly one axis, <code>credentialOwner</code>: who holds the credential and is responsible for keeping it valid.</p><table tabindex="0"><thead><tr><th><code>credentialOwner</code></th><th>Who holds the credential</th><th>Written by</th></tr></thead><tbody><tr><td><code>none</code></td><td>Nobody — <code>secretNames</code> is empty, there is nothing to connect or disconnect</td><td><code>POST /api/v1/connectors/custom</code></td></tr><tr><td><code>static</code></td><td>A human pasted a value; it is valid until someone replaces it</td><td><code>POST /api/v1/connectors/custom</code>, <code>POST /:id/connect</code></td></tr><tr><td><code>gateway</code></td><td>This gateway ran the OAuth flow, holds the <code>refresh_token</code>, and renews the <code>access_token</code> itself. The only value the refresh sweep acts on</td><td><code>POST /api/v1/connectors/custom</code> with <code>oauth: true</code></td></tr><tr><td><code>external</code></td><td>An external control plane owns the sign-in and pushes fresh tokens in; the gateway never refreshes</td><td><code>POST /:id/oauth/receive</code></td></tr></tbody></table><p>The field is written once, by the route that creates the entry, and read verbatim everywhere after — nothing re-derives it at read time.</p><p>An entry is raw <code>mcpServers</code>-entry JSON with <code>{placeholder}</code> tokens standing in for secrets, e.g.:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;streamable-http&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;url&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://mcp.example.com/v2/mcp&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;headers&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;Authorization&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bearer {api_key}&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>At add time the gateway extracts every <code>{name}</code> placeholder into <code>secretNames</code>; at spawn it substitutes each one from the secret store. A connector counts as <strong>connected</strong> when every one of its <code>secretNames</code> has a value — the secret store alone answers that question, never <code>config.json</code>.</p><p><strong>Secret storage.</strong> Values live in <code>~/.claude-gateway/mcp-token.env</code>, a <code>KEY=value</code> file at mode <code>0600</code>, parsed fresh on every read (a &quot;connect&quot; takes effect on the next session spawn, with no gateway restart). Connector keys are namespaced <code>CUSTOM__&lt;connectorId&gt;__&lt;placeholderName&gt;</code> so two connectors that both use <code>{api_key}</code> cannot collide. Override the path with <code>GATEWAY_MCP_TOKEN_ENV_PATH</code> (used by tests).</p><blockquote><p>A reserved second namespace, <code>CUSTOMINT__&lt;connectorId&gt;__&lt;name&gt;</code>, holds the gateway&#39;s own OAuth bookkeeping (refresh token, client id, expiry, failure counters). Placeholder names beginning with <code>__</code> are rejected at add time, and the two namespaces are separate prefixes so a pasted <code>{__refresh_token}</code> cannot resolve to gateway-internal state even if a future caller forgets to validate.</p></blockquote><p><strong>Connector ids</strong> are slugs: <code>^[a-z0-9][a-z0-9-]*$</code>, max 64 characters. Ids are used directly as <code>config.json</code> object keys and are interpolated into secret-file key names, so every route validates the shape of <code>:id</code> before touching either — anything else is <code>400 Invalid connector id</code>, ahead of any lookup.</p><p><strong>Auth levels:</strong> read routes take any valid API key; every mutating route requires an <strong>admin</strong> key (<code>admin: true</code>). The gateway mounts connector management routes only when <code>gateway.api.keys</code> is configured.</p><hr><h2 id="get-apiv1connectors" tabindex="-1">GET /api/v1/connectors <a class="header-anchor" href="#get-apiv1connectors" aria-label="Permalink to &quot;GET /api/v1/connectors {#get-apiv1connectors}&quot;">​</a></h2><p>List every connector with its current connected state.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -s</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;X-Api-Key: admin-key-456&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  http://localhost:10850/api/v1/connectors</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> jq</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;connectors&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;firecrawl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;label&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Firecrawl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;description&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Web scraping and crawling&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;credentialOwner&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;gateway&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;connected&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;repoUrl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://firecrawl.dev&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;refresh&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;consecutiveFailures&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;permanentFailures&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;nextAttemptAt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1893456000000</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td><code>credentialOwner</code></td><td><code>none</code> | <code>static</code> | <code>gateway</code> | <code>external</code> — see the table above. Also tells a UI which way to offer connecting: a paste-token box (<code>static</code>), a &quot;Sign in&quot; link pointing at <code>/oauth/start</code> (<code>gateway</code>), or neither</td></tr><tr><td><code>connected</code></td><td>Every required secret is present (always <code>true</code> for <code>credentialOwner: &quot;none&quot;</code>)</td></tr><tr><td><code>refresh</code></td><td><strong>Absent when healthy.</strong> Present only while a <code>gateway</code>-owned connector&#39;s background token refresh is failing — either transiently (a network error, DNS, a 5xx) or because the authorization server itself refused the grant. See below. Never present for any other owner: this gateway holds a <code>refresh_token</code> for none of them.</td></tr></tbody></table><p><code>connected</code> is computed purely from secret presence, and a transient refresh failure deliberately never deletes the stored token (an outage must not destroy a valid sign-in). So a connector whose provider disappeared hours ago still reports <code>connected: true</code> while every call through its expired token fails. <code>refresh</code> is what lets a UI say &quot;connected, but refreshing is failing&quot; instead of showing an indefinite green checkmark:</p><table tabindex="0"><thead><tr><th>Field</th><th>Description</th></tr></thead><tbody><tr><td><code>refresh.consecutiveFailures</code></td><td>Transient failures in a row; resets to <code>0</code> on the first success, and on any answer from the authorization server (including a refusal — that proves the host is reachable)</td></tr><tr><td><code>refresh.permanentFailures</code></td><td>Refusals from the authorization server in a row (<code>invalid_grant</code> and friends). At <code>3</code> the sweep gives up and deletes the connector&#39;s credentials, so <code>2</code> means one tick away from being disconnected — the most actionable value this block can carry. Resets to <code>0</code> on the first success.</td></tr><tr><td><code>refresh.nextAttemptAt</code></td><td>Epoch ms. The sweep skips this connector until then</td></tr><tr><td><code>refresh.unrefreshable</code></td><td>Present and <code>true</code> only when this connector can never refresh: it has an access_token, no refresh_token was ever stored, and the token is either expired or has no recorded expiry at all. Both counters read <code>0</code> — nothing failed, there is simply nothing to refresh with. Reconnecting is the only fix.</td></tr></tbody></table><p>The two counters are mutually exclusive: a refusal ends the transient streak and a network failure ends the permanent one, so exactly one of them is non-zero at a time. The block is present whenever either is — or when <code>unrefreshable</code> is set, which is the one case where both counters are <code>0</code>.</p><p><code>unrefreshable</code> is reachable through ordinary configuration, not just a hand-edited file: an authorization server that advertises scopes not including <code>offline_access</code> issues a token response with no refresh_token at all. The sweep then skips that connector on every tick, silently and forever, and without this flag <code>connected</code> would stay <code>true</code> over a token that expired an hour in.</p><p>The missing-expiry case covers tokens this gateway never minted — every path that stores one records an expiry beside it, defaulting to an hour when the server omits <code>expires_in</code>. An <code>oauth: true</code> connector holding a pasted <code>access_token</code> is the way to get there; <code>POST /api/v1/connectors/custom</code> now rejects that combination, so this reports the rows that predate the check.</p><p>The retry interval doubles with each consecutive transient failure — 5m, 10m, 20m, … capped at 6 hours — so a permanently dead MCP URL costs a handful of attempts a day rather than one every five minutes forever, and still recovers on its own the moment the provider answers again. There is no give-up state for transient failures: only the authorization server explicitly declaring the grant dead (<code>invalid_grant</code> and friends, three times running) clears the credentials.</p><p><strong>Errors.</strong> This route answers <code>500 {&quot;error&quot;: &quot;Connector configuration could not be read&quot;}</code> if the connector list cannot be assembled at all; the reason is logged, not returned. A single unreadable <em>entry</em> degrades to one row with <code>connected: false</code> instead, and the rest of the list is still returned. If the secret store itself (<code>mcp-token.env</code>) cannot be read, every connector reports <code>connected: false</code> — the honest answer while the file is unreadable — and the failure is logged at most once a minute rather than once per poll.</p><hr><h2 id="get-apiv1connectorsidstatus" tabindex="-1">GET /api/v1/connectors/:id/status <a class="header-anchor" href="#get-apiv1connectorsidstatus" aria-label="Permalink to &quot;GET /api/v1/connectors/:id/status {#get-apiv1connectorsidstatus}&quot;">​</a></h2><p>Connected state for one connector — cheap enough to poll while an OAuth sign-in completes in another tab.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;firecrawl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;connected&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p>A <code>gateway</code>-owned connector whose background refresh is currently failing also carries the same <code>refresh</code> block documented under <code>GET /api/v1/connectors</code> above:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;firecrawl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;connected&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;refresh&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;consecutiveFailures&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;permanentFailures&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;nextAttemptAt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1893456000000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><code>404</code> when the id is not a configured connector.</p><hr><h2 id="post-apiv1connectorsidconnect" tabindex="-1">POST /api/v1/connectors/:id/connect <a class="header-anchor" href="#post-apiv1connectorsidconnect" aria-label="Permalink to &quot;POST /api/v1/connectors/:id/connect {#post-apiv1connectorsidconnect}&quot;">​</a></h2><p>Store a pasted token into a connector that declares exactly one secret. <strong>Admin.</strong> This is what lets a paste-token connector be reconnected after <code>DELETE</code> soft-disconnected it, without retyping its config.</p><p><strong>Request body:</strong></p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>token</code></td><td>string</td><td>The secret. Trimmed; must be non-empty</td></tr></tbody></table><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;X-Api-Key: admin-key-456&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Content-Type: application/json&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{&quot;token&quot;: &quot;&lt;paste-token-here&gt;&quot;}&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  http://localhost:10850/api/v1/connectors/stripe/connect</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> jq</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;stripe&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;connected&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Status</th><th>When</th></tr></thead><tbody><tr><td><code>400</code></td><td>Missing/blank <code>token</code>; <code>credentialOwner: &quot;gateway&quot;</code> (use <code>/oauth/start</code>); <code>credentialOwner: &quot;external&quot;</code> (use <code>/oauth/receive</code>); or it needs more than one secret (remove and re-add it via <code>POST /api/v1/connectors/custom</code>)</td></tr><tr><td><code>404</code></td><td>Unknown id</td></tr></tbody></table><p>Sessions already running for an agent that uses this connector are restarted (idle channel sessions on their next message, busy ones after the current turn) so the new secret actually reaches them — an MCP subprocess reads its config once, at spawn.</p><hr><h2 id="post-apiv1connectorsidoauthreceive" tabindex="-1">POST /api/v1/connectors/:id/oauth/receive <a class="header-anchor" href="#post-apiv1connectorsidoauthreceive" aria-label="Permalink to &quot;POST /api/v1/connectors/:id/oauth/receive {#post-apiv1connectorsidoauthreceive}&quot;">​</a></h2><p>Accept a fresh <code>access_token</code> <strong>plus the full connector shape</strong> pushed in by an external control plane. <strong>Admin.</strong></p><p>This exists because an externally-owned connector never runs its token exchange here. The gateway runs inside the user&#39;s own VM, reachable from that user&#39;s own shell — a shared <code>client_secret</code> cannot live here safely. So a control plane the deployer runs owns the client secret, the exchange and the refresh loop, and pushes the resulting short-lived token to this route over the internal network, authenticated with an admin API key like any other admin caller.</p><p><strong>Request body:</strong></p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>access_token</code></td><td>string</td><td>Required, non-empty</td></tr><tr><td><code>label</code></td><td>string</td><td>Required, non-empty</td></tr><tr><td><code>config</code></td><td>object</td><td>Required. Must contain <strong>exactly one</strong> <code>{access_token}</code> placeholder and no others</td></tr><tr><td><code>defaultEnabled</code></td><td>boolean</td><td>Optional. <code>false</code> requires explicit per-agent opt-in. Omitted preserves existing gateway defaults; <code>true</code> never overrides <code>gateway.connectorsDefaultEnabled: false</code>. Returned in create/list/status responses when configured</td></tr><tr><td><code>description</code></td><td>string</td><td>Optional</td></tr><tr><td><code>sourceUrl</code></td><td>string</td><td>Optional</td></tr></tbody></table><p><code>secretNames</code> is never read from the request body — it is derived from <code>config</code> and required to be exactly <code>[&quot;access_token&quot;]</code>.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;X-Api-Key: admin-key-456&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Content-Type: application/json&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;access_token&quot;: &quot;&lt;token&gt;&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;label&quot;: &quot;Gmail&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;config&quot;: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          &quot;type&quot;: &quot;streamable-http&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          &quot;url&quot;: &quot;https://mcp.example.com/gmail&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          &quot;headers&quot;: { &quot;Authorization&quot;: &quot;Bearer {access_token}&quot; }</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        }</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      }&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  http://localhost:10850/api/v1/connectors/gmail/oauth/receive</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> jq</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;gmail&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;connected&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p>The entry is written to <code>gateway.customConnectors</code> with <code>credentialOwner: &quot;external&quot;</code>, and every session already using this connector is restarted so the next spawn picks up the fresh token.</p><p><strong><code>400</code> on a reserved id.</strong> This is the only route that takes a connector id verbatim instead of minting it through the slugifier, so it is the only one that can name a server the gateway writes into every session&#39;s <code>mcp-config.json</code> itself. <code>gateway</code> and <code>telegram</code> are therefore rejected:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;error&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Connector id &#39;gateway&#39; is reserved by the gateway&#39;s own MCP servers&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p>Without this the push would succeed, store its token, and report <code>connected: true</code> on every status surface — while the session writer silently dropped the colliding entry, so the connector never reached a single session and could not be fixed except by deleting it.</p><hr><h2 id="delete-apiv1connectorsid" tabindex="-1">DELETE /api/v1/connectors/:id <a class="header-anchor" href="#delete-apiv1connectorsid" aria-label="Permalink to &quot;DELETE /api/v1/connectors/:id {#delete-apiv1connectorsid}&quot;">​</a></h2><p>Disconnect. <strong>Admin.</strong> Admin is checked <em>before</em> the id is looked up, so a non-admin caller cannot use this route to learn which ids exist.</p><p>What it removes depends on the entry:</p><table tabindex="0"><thead><tr><th><code>credentialOwner</code></th><th>Effect</th></tr></thead><tbody><tr><td><code>static</code></td><td>Clears the secrets only — the entry (label, config, <code>sourceUrl</code>) survives so it can be reconnected without retyping. The definition exists nowhere else</td></tr><tr><td><code>gateway</code></td><td>Clears the OAuth <code>access_token</code> only (plus the internal bookkeeping below); the entry survives, and so does any <em>other</em> <code>{placeholder}</code> value that was pasted when the connector was added. Only <code>access_token</code> is the gateway&#39;s to re-mint at sign-in — a <code>{workspace_id}</code> alongside it can be re-supplied by no route at all (<code>/connect</code> is closed to <code>gateway</code> owners), so clearing it would make one Disconnect permanent</td></tr><tr><td><code>none</code></td><td>Removes the entry. There is no secret to clear, so a soft disconnect would leave the row reporting &quot;connected&quot; forever</td></tr><tr><td><code>external</code></td><td>Removes the entry — its definition lives in the control plane, and reconnecting re-pushes a full entry via <code>/oauth/receive</code></td></tr></tbody></table><p>For a <code>gateway</code>-owned entry the gateway also clears its refresh token, client id, expiry, failure counters, token generation and cached dynamic-client registration. Those are internal bookkeeping and are not in <code>secretNames</code>; left behind, the still-valid refresh token would let the background sweep silently mint a new access token and resurrect the connector the user just disconnected, and a stale cached registration would make the next <code>/oauth/start</code> reuse a <code>client_id</code> the provider may no longer recognise.</p><p>Sessions already using the connector are restarted so they stop offering a tool whose credential is gone. Which sessions those are is decided by comparing each running session&#39;s spawn-time connector fingerprint against what the connector resolves to <em>now</em> — so a cleared secret, an edited config, or a removed entry all count, and a session spawned before the connector existed is left alone. When the whole entry is removed, the connector&#39;s per-agent enablement flags in <code>config.json</code> are removed with it, so a later connector that slugs to the same id does not inherit them.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;firecrawl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;connected&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><hr><h2 id="post-apiv1connectorscustom" tabindex="-1">POST /api/v1/connectors/custom <a class="header-anchor" href="#post-apiv1connectorscustom" aria-label="Permalink to &quot;POST /api/v1/connectors/custom {#post-apiv1connectorscustom}&quot;">​</a></h2><p>Add a user-pasted connector. <strong>Admin.</strong></p><p>Before relying on per-connector opt-in, clients should check that <code>GET /api/v1/connectors</code> returns <code>capabilities.perConnectorDefaults: true</code>. Older gateways may ignore unknown fields. Store a personal browser connector with <code>defaultEnabled: false</code>, then enable only selected agents through the existing agent PATCH endpoint.</p><p><strong>Request body:</strong></p><table tabindex="0"><thead><tr><th>Field</th><th>Type</th><th>Description</th></tr></thead><tbody><tr><td><code>label</code></td><td>string</td><td>Required, non-empty. The id is slugified from it (<code>&quot;Google Calendar!&quot;</code> → <code>google-calendar</code>), with <code>-2</code>, <code>-3</code>, … appended on collision</td></tr><tr><td><code>config</code></td><td>object</td><td>Required. Raw <code>mcpServers</code> entry with <code>{placeholder}</code> tokens</td></tr><tr><td><code>secrets</code></td><td>object</td><td>Optional <code>{ &quot;&lt;placeholderName&gt;&quot;: &quot;&lt;value&gt;&quot; }</code>. All values must be strings; blank ones are skipped, leaving the connector &quot;not connected&quot; until filled in later via <code>/connect</code></td></tr><tr><td><code>description</code></td><td>string</td><td>Optional</td></tr><tr><td><code>sourceUrl</code></td><td>string</td><td>Optional — where the admin says the config came from. Unverified</td></tr><tr><td><code>oauth</code></td><td>boolean</td><td>Optional. Asks the gateway to run the sign-in itself — stores <code>credentialOwner: &quot;gateway&quot;</code>. Requires <code>config.url</code> and an <code>{access_token}</code> placeholder, and refuses an <code>access_token</code> in <code>secrets</code> (the gateway mints that one at <code>/oauth/start</code>). Without it the entry is <code>static</code> (it declares <code>{placeholder}</code>s) or <code>none</code> (it declares none)</td></tr></tbody></table><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;X-Api-Key: admin-key-456&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Content-Type: application/json&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;label&quot;: &quot;Firecrawl&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;oauth&quot;: true,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        &quot;config&quot;: {</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          &quot;type&quot;: &quot;streamable-http&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          &quot;url&quot;: &quot;https://mcp.firecrawl.dev/v2/mcp-oauth&quot;,</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">          &quot;headers&quot;: { &quot;Authorization&quot;: &quot;Bearer {access_token}&quot; }</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">        }</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">      }&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  http://localhost:10850/api/v1/connectors/custom</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> jq</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;firecrawl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;label&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Firecrawl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;connected&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Status</th><th>When</th></tr></thead><tbody><tr><td><code>400</code></td><td><code>label</code>/<code>config</code> missing or the wrong type; <code>secrets</code> not an object of strings; <strong>a <code>secrets</code> key that is not a <code>{placeholder}</code> in <code>config</code></strong> (nothing would ever read it back, so it is reported instead of silently stored); <code>oauth</code> not a boolean; <code>oauth: true</code> without <code>config.url</code> or without an <code>{access_token}</code> placeholder; <strong><code>oauth: true</code> with an <code>access_token</code> in <code>secrets</code></strong> (see below); <strong>or a placeholder name starting with <code>__</code>, which the gateway reserves for itself</strong></td></tr></tbody></table><p>The id is chosen inside the config write lock, so two concurrent adds of the same label get distinct ids rather than the second overwriting the first. If every secret is present the connector is immediately connected, and sessions that resolve it are restarted.</p><p><strong><code>oauth: true</code> will not take a pasted <code>access_token</code>.</strong> That combination asks for a <code>gateway</code>-owned entry — one the refresh sweep renews from the <code>refresh_token</code> the sign-in stores — and a pasted token arrives without one, because the gateway never saw the exchange it came out of. It would read <code>connected: true</code> and then simply stop working when it aged out, with nothing to renew it and no failure recorded (the sweep skips a connector it cannot refresh). Sign in via <code>/oauth/start</code>, or omit <code>oauth</code> to store the token as a <code>static</code> connector, which is what a hand-held token is. The connector&#39;s <em>other</em> placeholders are unaffected — a <code>{workspace_id}</code> on an OAuth connector is configuration the sign-in neither writes nor can supply.</p><p>Removal is the unified <code>DELETE /api/v1/connectors/:id</code> above — there is no separate <code>/custom/:id</code> delete route.</p><hr><h2 id="post-apiv1connectorscustomidoauthstart" tabindex="-1">POST /api/v1/connectors/custom/:id/oauth/start <a class="header-anchor" href="#post-apiv1connectorscustomidoauthstart" aria-label="Permalink to &quot;POST /api/v1/connectors/custom/:id/oauth/start {#post-apiv1connectorscustomidoauthstart}&quot;">​</a></h2><p>Begin OAuth sign-in for a <code>gateway</code>-owned connector (added with <code>oauth: true</code>). <strong>Admin.</strong></p><p>Unlike <code>/oauth/receive</code>, the whole dance runs <strong>here</strong>, inside the user&#39;s own VM — RFC 8414 metadata discovery, RFC 7591 dynamic client registration, PKCE (S256), the code exchange and the refresh loop. No external service ever sees the resulting token.</p><p>Requires <a href="/claude-gateway/reference/configuration.html"><code>gateway.publicUrl</code></a> to be set: the provider needs a reachable HTTPS callback, which is <code>&lt;publicUrl&gt;/oauth/mcp/callback</code>.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;X-Api-Key: admin-key-456&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  http://localhost:10850/api/v1/connectors/custom/firecrawl/oauth/start</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> jq</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;authorizeUrl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://as.example.com/authorize?response_type=code&amp;client_id=...&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p>Open <code>authorizeUrl</code> in the end user&#39;s browser. A <code>client_id</code> registered by a previous attempt is reused rather than orphaning a new one at the provider on every click — but only while the <code>redirect_uri</code> it was registered against still matches, so changing <code>gateway.publicUrl</code> correctly forces re-registration.</p><p>The gateway registers a client dynamically (RFC 7591) whenever the provider advertises a <code>registration_endpoint</code>. For a provider that advertises none, set a pre-registered <code>client_id</code> in the <code>MCP_OAUTH_CLIENT_ID__&lt;CONNECTOR_ID&gt;</code> environment variable (id upper-cased, every non-alphanumeric run replaced with <code>_</code> — connector <code>google-calendar</code> reads <code>MCP_OAUTH_CLIENT_ID__GOOGLE_CALENDAR</code>). Without either, <code>/oauth/start</code> returns <code>502</code> naming the env var it looked for.</p><p>The <code>scope</code> requested is the MCP server&#39;s own <code>scopes_supported</code> (RFC 9728 protected-resource metadata) when it publishes one, falling back to the authorization server&#39;s list (RFC 8414) and then to <code>offline_access</code>. The resource&#39;s list comes first deliberately: the AS&#39;s is every scope it issues for every resource behind it, so consenting to it would grant this gateway a whole provider&#39;s privileges — and on an AS whose catalogue includes scopes this client is not entitled to, it is an <code>invalid_scope</code> refusal that kills the sign-in. Set <code>MCP_OAUTH_SCOPES__&lt;CONNECTOR_ID&gt;</code> (space-separated, same id transform as <code>MCP_OAUTH_CLIENT_ID__</code>) to override both — that is the way out of an <code>invalid_scope</code>, or of a provider that only issues a refresh token when <code>offline_access</code> is asked for and never advertises it, without patching the gateway.</p><table tabindex="0"><thead><tr><th>Status</th><th>When</th></tr></thead><tbody><tr><td><code>400</code></td><td>The connector is not <code>credentialOwner: &quot;gateway&quot;</code> (an <code>external</code> one is told to use <code>/oauth/receive</code> instead), or its <code>config.url</code> is missing</td></tr><tr><td><code>404</code></td><td>Unknown id</td></tr><tr><td><code>500</code></td><td>No valid <code>gateway.publicUrl</code> configured</td></tr><tr><td><code>502</code></td><td>Discovery or client registration failed upstream</td></tr></tbody></table><hr><h2 id="get-oauthmcpcallback" tabindex="-1">GET /oauth/mcp/callback <a class="header-anchor" href="#get-oauthmcpcallback" aria-label="Permalink to &quot;GET /oauth/mcp/callback {#get-oauthmcpcallback}&quot;">​</a></h2><p>The provider&#39;s redirect target. <strong>Public — no API key</strong>, because the end user&#39;s browser has none to present. Its security rests on the <code>state</code> value: single-use, TTL&#39;d and unguessable, the same posture the CLI pairing routes already use.</p><p>Registered at <strong>both</strong> <code>/oauth/mcp/callback</code> and <code>/gateway/oauth/mcp/callback</code>. The <code>redirect_uri</code> sent to the provider is <code>&lt;publicUrl&gt;/oauth/mcp/callback</code>, and <code>publicUrl</code> always ends in <code>/gateway</code> — so behind the usual reverse proxy, which strips that prefix, the request arrives at the bare path, while a gateway reached directly on its own port receives the prefixed one. Both are the same handler; a flow started against one and returning to the other completes normally.</p><p>Query params are the standard <code>code</code> / <code>state</code> / <code>error</code>. On success the gateway exchanges the code, stores the access token (plus refresh token and expiry, internally) in a single write, and restarts sessions that use the connector so the sign-in reaches the agent the user is actually talking to — without that, status would report &quot;connected&quot; while a running session still had no such tool. A restart failure is logged, not surfaced: the token is stored either way.</p><p>The connector is re-read before the exchange and again immediately before the write. If it was deleted, or handed to another owner via <a href="/claude-gateway/api/connectors.html#post-apiv1connectorsidoauthreceive"><code>/oauth/receive</code></a>, while the user sat on the provider&#39;s consent screen, the token is discarded and the callback answers <code>connector_gone</code> — nothing is stored. Without that re-check a returning callback could resurrect a connector the admin had just disconnected, or leave internal refresh state behind on an entry the gateway no longer owns, which nothing would ever collect.</p><p>Where the browser lands depends on <a href="/claude-gateway/reference/configuration.html"><code>gateway.oauthReturnUrl</code></a>:</p><table tabindex="0"><thead><tr><th><code>oauthReturnUrl</code></th><th>Success</th><th>Failure</th></tr></thead><tbody><tr><td>Set</td><td><code>302</code> to that URL</td><td><code>302</code> to that URL with <code>?connector_oauth_error=&lt;code&gt;</code></td></tr><tr><td>Unset</td><td>A plain &quot;Connected — you can close this tab&quot; page</td><td>A plain error page (<code>400</code>/<code>409</code>/<code>502</code>)</td></tr></tbody></table><p>&quot;Set&quot; means set to a well-formed <code>http(s)</code> URL — anything else (including a <code>javascript:</code> or <code>data:</code> URL, which <code>new URL()</code> parses happily) is logged at startup and treated as unset, so it can never become the <code>Location</code> of a redirect on this public route.</p><p>Error codes are <code>expired_link</code> (unknown, expired or already-used <code>state</code>), <code>missing_code</code>, <code>connector_gone</code>, <code>exchange_failed</code>, or the provider&#39;s own <code>error</code> value passed through.</p><p>There is deliberately no interstitial &quot;Connected!&quot; page with a timed meta-refresh — when the deployer has told the gateway where &quot;back&quot; is, a real redirect goes straight there.</p><hr><h2 id="token-refresh" tabindex="-1">Token refresh <a class="header-anchor" href="#token-refresh" aria-label="Permalink to &quot;Token refresh {#token-refresh}&quot;">​</a></h2><p>For <code>oauth: true</code> connectors the gateway refreshes tokens itself, on the same 60-second interval that prunes pairing state. A connector is refreshed when its recorded expiry is within 5 minutes.</p><ul><li>Failures back off for 5 minutes rather than retrying every tick.</li><li>After <strong>3</strong> consecutive failures <strong>that the authorization server itself declared</strong> (<code>invalid_grant</code>, <code>invalid_client</code>, <code>unauthorized_client</code>, <code>invalid_scope</code>) the gateway gives up and clears the connector&#39;s tokens, so its status honestly flips to &quot;not connected&quot; instead of showing a green checkmark backed by a token that will never refresh again. Anything else — DNS, a timeout, a <code>502</code>, a discovery step that returned the wrong shape — only backs off and never counts toward that limit: giving up deletes the user&#39;s credentials, and a provider being unreachable says nothing about whether the grant is still valid.</li><li>Discovery metadata is cached for 6 hours on this path (an admin-initiated <code>/oauth/start</code> always re-discovers).</li><li>A tick is skipped while the previous sweep is still in flight. Two concurrent sweeps would POST the same refresh token twice, and a provider that rotates refresh tokens (the default for a public OAuth 2.1 client) treats the second use as replay and revokes the whole grant.</li><li>If a manual reconnect lands while a refresh is in flight, the newer token wins — the slower, now-stale refresh result is discarded rather than clobbering it.</li><li>A successful refresh restarts sessions using that connector, for the same reason <code>/oauth/receive</code> does.</li></ul><hr><h2 id="connector-resources" tabindex="-1">Connector resources <a class="header-anchor" href="#connector-resources" aria-label="Permalink to &quot;Connector resources {#connector-resources}&quot;">​</a></h2><p>Admin-created HTTP connectors may set <code>resourcesPath</code> (a simple same-origin path such as <code>/v1/grants</code>). <code>GET /api/v1/connectors/:id/resources</code> lists those resources; <code>DELETE /api/v1/connectors/:id/resources/:resourceId</code> revokes one. Both require connector-admin access, use stored credentials without exposing them to the caller, reject redirects, and have a 10-second network timeout. The list endpoint advertises <code>capabilities.connectorResources</code>. <code>DELETE /api/v1/connectors/:id?remove=true</code> explicitly removes the connector definition and its agent flags as well as credentials; ordinary DELETE retains its existing behavior. Revoke remote resources before removing their local credentials.</p><p>For existing HTTP connectors, admins can PATCH <code>/api/v1/connectors/:id/management</code> with <code>{ &quot;resourcesPath&quot;: &quot;/v1/grants&quot; }</code> without replacing credentials. Model provider capacity/unavailability is reported as HTTP 503 by the message API and with an actionable provider message in chat.</p>`,104)])])}const u=t(n,[["render",o]]);export{k as __pageData,u as default};
