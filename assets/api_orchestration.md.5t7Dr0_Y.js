import{_ as s,o as t,c as a,a2 as i}from"./chunks/framework.D1JEzK2q.js";const k=JSON.parse('{"title":"Orchestration and live voice","description":"","frontmatter":{},"headers":[],"relativePath":"api/orchestration.md","filePath":"api/orchestration.md"}'),n={name:"api/orchestration.md"};function o(r,e,d,c,l,h){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="orchestration-and-live-voice" tabindex="-1">Orchestration and live voice <a class="header-anchor" href="#orchestration-and-live-voice" aria-label="Permalink to &quot;Orchestration and live voice {#orchestration-and-live-voice}&quot;">​</a></h1><p>Orchestration is enabled gateway-wide with <code>gateway.orchestration</code> and automatically sets and saves <code>gateway.headless: true</code>. See <a href="/claude-gateway/guide/orchestration.html">configuration, worker pooling and compatibility limits</a>. Existing message/session endpoints retain their authentication and session ownership rules. An initial message response may acknowledge a queued task; it is not proof of task completion. Worker results arrive later in the original conversation.</p><p>Telegram orchestration publishes an editable tool-activity message in the originating chat/topic. It uses the legacy Telegram layout: up to four previous details prefixed with <code>☑️ :</code>, the current detail prefixed with <code>🕐 :</code> when history exists, and <code>(elapsed: …)</code>. Agent and Worker tools share readable legacy labels; task IDs, raw MCP names and historical task lists are omitted. Details remain bounded and redacted. Updates are coalesced (at least four seconds apart per message) and respect Telegram retry-after responses. The message is deleted when all managed work in that conversation is idle. Elapsed-only updates run every ten seconds; tool detail changes can update sooner. This best-effort status is separate from durable task/result delivery and never controls worker execution; it does not replay inactive history at startup. Restarting during active work may create a new status message.</p><p>Worker reporting decisions receive the complete persisted results for their assigned notifications, including tasks older than the recent-task index. Other tasks appear as an index with <code>resultAvailable</code> and a <code>task_status</code> reference, not a shortened report. MCP <code>task_status</code> with <code>task_id</code> returns the full retained result and evidence; without it, the tool returns the decision&#39;s task index/current reports. Historical command receipts reference results instead of repeating their bodies. Worker final text is retained without the former 1,024/8,192-character cuts, up to the managed-turn limit of 256 KiB; larger output fails explicitly rather than being silently shortened. State events over their 64 KiB budget omit the result body and carry <code>resultAvailable</code>/<code>resultRef</code>; the authoritative task and attempt still retain it. Git diff previews remain explicitly marked with <code>diff.truncated</code> and are separate from worker final text.</p><p>Worker failures retain a bounded, redacted <code>failure</code> object (<code>code</code>, <code>message</code>, <code>observedAt</code>) on the task and attempt. Gateway shutdown is reported as <code>GATEWAY_SHUTDOWN</code>, rather than an unexplained issue failure. Task-specific <code>task_status</code> and current notification reports include this evidence and the latest eight tool activity entries (tool name, use/result, error flag and timestamp). Uncertain process outcomes remain <code>needs_reconciliation</code>; failure evidence does not authorize replaying side effects. Older failed tasks without stored evidence are not retroactively assigned a cause.</p><p>Managed Agent timeout diagnostics are emitted as <code>response.timeout</code> with <code>responseId</code>, <code>phase</code> (<code>startup</code>, <code>first_response</code>, <code>idle</code>, or <code>total</code>), <code>elapsedMs</code>, and <code>idleMs</code>. The existing <code>response.failed</code> event still closes a failed turn. <code>conversation.idleTimeoutMs</code> limits inactivity after inference starts; the legacy <code>decisionTimeoutMs: 15000</code> template value no longer overrides the 120000 default; startup/first-response budgets are separate and <code>maxDecisionDurationMs</code> remains a hard cap. Shorter API request deadlines remain enforced. No automatic replay of side effects is introduced.</p><p>Managed activity is based on durable input, decision and Worker states, including autonomous result turns. A completed Agent acknowledgement does not make a running Worker idle. Telegram/Discord/LINE typing renewal stops at idle or user-input/reconciliation waits; legacy file-heartbeat warnings and replay recovery are excluded for managed turns. API/voice retain their own request, process and transport deadlines.</p><p>Uploaded still images (PNG/JPEG/GIF/WebP) are supplied as native image content in the Agent&#39;s input turn before any task dispatch. Existing attachment upload and authorization rules are unchanged. The Agent can answer image questions directly; workers still inherit original attachment references for subsequent execution. Native image input is bounded to 5 MiB per image, 20 MiB and 20 images per turn. Unsupported, missing or over-limit images are identified to the Agent instead of silently treating a filename as visible image content.</p><h2 id="session-activity" tabindex="-1">Session activity <a class="header-anchor" href="#session-activity" aria-label="Permalink to &quot;Session activity {#session-activity}&quot;">​</a></h2><p><code>GET /api/v1/agents/:agentId/sessions/:sessionId/activity?after=0</code></p><p>Requires an API key authorized for the Agent and the conversation principal. <code>after</code> is a non-negative event cursor; pass the previous response&#39;s <code>cursor</code> on the next poll. Invalid session IDs/cursor syntax return <code>400</code>; inaccessible activity returns <code>403</code>. The response contains current bounded task/response snapshots and incremental tool activity (at most 500 tool events per poll):</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;cursor&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">12</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;busy&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;tasks&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;taskId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;task-id&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;title&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Review change&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;state&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;completed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;stateVersion&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;result&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Review finished&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;updatedAt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1788760000000</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;responses&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;response-id&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;requestId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">null</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;state&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;completed&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;text&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Review finished&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;createdAt&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1788760000000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;files&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: []</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;tools&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;seq&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">12</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;tool_use&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;tool-use-id&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;name&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bash&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;input&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;command&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;npm test&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;taskId&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;task-id&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;role&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;worker&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Activity snapshots retain all non-terminal tasks plus the latest 100 terminal tasks, so newer completed work cannot hide an older running task. <code>/tasks</code> paginates active work and <code>/stop</code> selects cancellable work independently of terminal history.</p><p>Deduplicate responses by <code>id</code> and tool events by <code>seq</code>; snapshots are repeated across polls. Background responses may have <code>requestId: null</code>. <code>busy</code> describes the conversational Agent turn, so also inspect task states for running workers. <code>tool_result</code> events identify the call and may include <code>is_error</code>; this feed does not expose raw reasoning or complete tool-result bodies. Files use the existing media delivery API. Old events are pruned independently of task snapshots.</p><h2 id="internal-task-dispatch" tabindex="-1">Internal task dispatch <a class="header-anchor" href="#internal-task-dispatch" aria-label="Permalink to &quot;Internal task dispatch {#internal-task-dispatch}&quot;">​</a></h2><p><strong>Telegram tool status.</strong> While managed work is active, its tool-status message moves to the bottom after six newer gateway-observed user messages or delivered replies in the same account/chat/topic, at most once per 30 seconds. The replacement is sent silently before the previous message is deleted. Failed replacement preserves the old message; failed deletion retains its ID for retry and blocks additional replacements. Synthetic task-report inputs do not count. This approximates chat activity, not the user&#39;s scroll position. Legacy mode is unchanged.</p><p><strong>Orchestration skills.</strong> Agent catalogs combine gateway workspace/module/shared skills with metadata discovered from the installed Claude Code runtime through an initialization-only handshake (no model request). Native/bundled skills such as <code>/code-review</code> and their reported aliases run directly through the worker&#39;s <code>Skill</code> tool; gateway shared/workspace skills retain their supplied files/resources. Gateway definitions take precedence for the same name. Discovery is cached briefly and refreshed before routing, while workers check availability in their actual execution environment. App discovery runs inside the validated container without host fallback. If discovery fails, ordinary gateway skills remain available and the Agent must report incomplete discovery rather than inventing a missing-skill or MCP-inventory diagnosis.</p><p>The Agent&#39;s scoped MCP <code>task_spawn</code> tool takes <code>title</code>, <code>instructions</code>, <code>target_profile</code>, optional <code>context_refs</code>, and optional <code>continue_task_id</code>/<code>continuation_policy</code>. A new continuation defaults to <code>after_success</code>: it is queued durably and starts only after its predecessor completes successfully. Failed/cancelled predecessors cause <code>TASK_DEPENDENCY_FAILED</code> without starting the dependent worker. Explicit <code>after_terminal</code> is for authorized recovery/inspection after a terminal outcome; uncertain or still-running predecessors continue to block it. Existing persisted tasks without the policy retain their previous terminal-only behavior. Skill dispatch also uses the registered <code>skill_name</code>/<code>skill_args</code>; live-voice delegation requires a contextual <code>spoken_acknowledgement</code>. These are model-facing tool fields, <strong>not additional fields on the public messages endpoint</strong>.</p><p>A continuation must reference a task in the same conversation. The gateway derives the workstream, queues behind its unfinished work and resumes a compatible worker session when available. Receipts/status retain <code>taskId</code>, <code>workstreamId</code>, <code>continueTaskId</code> and, after assignment, <code>workerId</code>. Workers report progress, request input and stage files through attempt-scoped tools. A playback stop does not cancel a worker task. The internal bridge/tickets must not be exposed as a public host execution API.</p><p>Task command identity is scoped to the originating input rather than the temporary decision. After interruption, matching committed mutations return the original receipt even when inference produces a new tool-call ID. A conflicting mutation for an input that already committed commands returns <code>RECOVERY_COMMAND_CONFLICT</code>; inspect persisted task status and obtain a new user input before issuing new work. This is task-command deduplication, not a guarantee that arbitrary external connector side effects are idempotent.</p><p>Host orchestration Agents and host workers load enabled native connectors using the same configuration as legacy mode. Connector credentials are refreshed on each new managed subprocess; in-flight tasks are not restarted for connector changes. Read-only decisions, disabled tools, isolated workers and app containers do not receive host connectors. Channel token hot reload applies to subsequent delivery attempts, including file and speech transport.</p><h2 id="user-task-cancellation" tabindex="-1">User task cancellation <a class="header-anchor" href="#user-task-cancellation" aria-label="Permalink to &quot;User task cancellation {#user-task-cancellation}&quot;">​</a></h2><p>For orchestration-enabled API sessions, <code>POST /api/v1/agents/:agentId/sessions/:sessionId/stop</code> interrupts the Agent reply and returns <code>{ stopped, menuId, tasks: [{ taskId, title, state }], responseText }</code>. Render <code>responseText</code> as an assistant message. Sending <code>/stop</code> through the messages endpoint returns the same numbered question using the normal JSON/SSE reply contract. A subsequent numeric message selects from that frozen menu without inference; <code>0</code> dismisses it. Menus are bound to the authenticated principal and session, expire after five minutes, and are replaced by another <code>/stop</code> or dismissed by unrelated text. Task selection does not follow worker-slot reuse or list reordering. <code>cancel_requested</code> means stopping, not confirmed termination. Only the selected task is cancelled; completed side effects are not rolled back. Legacy <code>/stop</code> retains its existing response and behavior.</p><p>Telegram private chats get task buttons plus Dismiss. The receiver and gateway both gate the new control path on orchestration; stale callbacks cannot cancel tasks in another session or after mode changes.</p><p>Worker file sharing uses the same active-attempt path checks as file staging. Pass the original absolute path inside the active task workspace, an authorized input attachment, or this session&#39;s media directory. Do not copy files into the agent-wide media root. The private task bridge returns <code>ARTIFACT_PATH_DENIED</code> with a corrective <code>message</code> and <code>retryable: true</code>; retry only after correcting the path. The MCP share client preserves the bridge error code (including stale or unauthorized attempts) instead of replacing every error with <code>share_scope_denied</code>. Files outside scope remain inaccessible and container boundaries are unchanged.</p><h2 id="agent-orchestration-and-voice-configuration" tabindex="-1">Agent orchestration and voice configuration <a class="header-anchor" href="#agent-orchestration-and-voice-configuration" aria-label="Permalink to &quot;Agent orchestration and voice configuration {#agent-orchestration-and-voice-configuration}&quot;">​</a></h2><p><code>gateway.orchestration</code> is <code>true</code> or <code>false</code> for all agents and channels. <code>agents[].orchestration</code> holds conversation/task/event tuning. All speech settings live directly at <code>agents[].voice</code>: <code>enabled</code>, <code>stt</code>, <code>tts</code>, <code>notes</code>, <code>language</code>, <code>turns</code>, and <code>playback</code>. No gateway voice defaults are inherited. New agents start disabled with no selected models. Voice requires both orchestration and the agent&#39;s voice switch; channel reply policy remains independently controlled by <code>/voice</code>.</p><p>Orchestration text delivery covers Telegram, Discord, LINE, Slack, WhatsApp (linked Baileys accounts and Cloud API), and WeChat. Linked channels reuse their running gateway transport; WhatsApp replies retain the receiving account in the durable binding. WhatsApp supports staged image/document delivery. WeChat currently supports text only; staged attachments report <code>CHANNEL_ATTACHMENTS_UNSUPPORTED</code>. WhatsApp and WeChat task/session menus use text commands (<code>/orch &lt;token&gt;</code>) instead of native buttons. Voice reply controls remain supported on Telegram, Discord, LINE and Slack; unsupported channels return an explicit notice.</p><p>Old object-form gateway settings and <code>agents[].orchestration.voice</code> are migrated once, preserving the effective values and explicit overrides in each agent before removing the old fields. Existing global task/conversation/event tuning is copied into each agent too. The migration is idempotent, preserves raw environment references, and coordinates with config writers. Provider API keys remain environment variables or upstream connector credentials.</p><h2 id="discord-line-and-slack-orchestration-controls" tabindex="-1">Discord, LINE and Slack orchestration controls <a class="header-anchor" href="#discord-line-and-slack-orchestration-controls" aria-label="Permalink to &quot;Discord, LINE and Slack orchestration controls {#discord-line-and-slack-orchestration-controls}&quot;">​</a></h2><p>The global <code>gateway.orchestration</code> switch applies to all Agents and all connected channels. Per-Agent settings only override tuning; per-Agent <code>enabled</code> and <code>channels</code> are ignored. <code>/session</code> and <code>/sessions</code> are direct gateway controls on these orchestration channels and do not invoke the model.</p><p>Enable <code>gateway.orchestration</code> and configure the same <code>voice.notes</code> / <code>voice.tts</code> fields as Telegram. <code>/voice [on|auto|off]</code>, <code>/voices</code>, <code>/tasks</code> and <code>/stop</code> are intercepted before inference. Settings retain the existing tables with namespaced non-Telegram keys (<code>[channel, chatId]</code>); Telegram keys remain unchanged. This prevents preferences for identical IDs on different platforms from colliding. Preferences apply across threads in a chat; delivery retains the original thread binding.</p><p>Explicit audio shares serve MP3 and M4A inline with <code>Accept-Ranges: bytes</code>, single-range <code>206</code> responses and <code>Content-Range</code> for player probes/seeking. GET and HEAD retain the existing token, containment and MIME checks; document shares remain downloads.</p><p>LINE loading uses <code>POST /v2/bot/chat/loading/start</code> for 1:1 user IDs. Ingress starts the indicator before media download; the orchestration runtime renews it from durable input/decision/task state, every four seconds with five-second expiry. A newly delivered acknowledgement triggers renewal on the next one-second activity tick if work remains. Idle, user-input wait, reconciliation, global Off and shutdown stop renewals. Loading renewals and outgoing orchestration replies/menus are serialized per Agent/chat, so a late renewal cannot overtake the reply that clears it. Ingress awaits its initial loading request before forwarding any command. Queued renewals recheck activity before sending. LINE clears loading on outgoing messages, has no explicit stop endpoint, and only shows it while the user is viewing the 1:1 chat; groups/rooms are unsupported. See <a href="https://developers.line.biz/en/reference/messaging-api/#display-a-loading-indicator" target="_blank" rel="noreferrer">LINE&#39;s loading API</a>.</p><p>Control tokens expire after five minutes and bind channel, chat, thread, session and principal. Replayed setting/cancellation selections, foreign users and session switches cannot reuse the token. Task views expose title/status/progress/question, not worker instructions, credentials or installed skill bodies. Discord and Slack edit the existing menu; LINE replaces quick replies with a new message and uses a valid reply token before push fallback.</p><table tabindex="0"><thead><tr><th>Ingress</th><th>Normalization and validation</th></tr></thead><tbody><tr><td>Discord receiver</td><td>Native slash commands/buttons re-run the access gate. Audio attachments carry <code>attachment_kind: voice</code>; only Discord CDN references are downloaded.</td></tr><tr><td><code>POST /webhooks/line/:agentId</code></td><td>Existing signed JSON route handles audio blobs and scoped control postbacks. Postbacks re-run the current source access policy.</td></tr><tr><td><code>POST /webhooks/slack/:agentId</code></td><td>Accepts signed JSON Events and signed URL-encoded slash-command / <code>block_actions</code> bodies. Audio downloads require HTTPS Slack hosts, bounded bytes and no credential-bearing redirects.</td></tr></tbody></table><p>Slack app registration remains an external setup step: point Slash Commands and Interactivity at the same webhook URL, enable <code>commands</code>, <code>files:read</code>, <code>files:write</code> alongside existing messaging scopes, and reinstall after scope changes. The bot token does not administer Slack autocomplete registrations, so disabled orchestration rejects stale commands but cannot remove them from Slack’s app settings. See <a href="https://docs.slack.dev/interactivity/implementing-slash-commands/" target="_blank" rel="noreferrer">Slack slash commands</a> and <a href="https://docs.slack.dev/interactivity/handling-user-interaction/" target="_blank" rel="noreferrer">interaction payloads</a>. Native slash commands are unavailable inside Slack threads; <code>@bot /command</code> messages preserve the thread context.</p><p>Speech uses a bounded MP3 file pinned to the selected provider/voice/model. Discord uploads an audio attachment; Slack uses <a href="https://docs.slack.dev/messaging/working-with-files/" target="_blank" rel="noreferrer">external file upload</a> and completes it in the original thread; LINE sends an <a href="https://developers.line.biz/en/reference/messaging-api/#audio-message" target="_blank" rel="noreferrer">audio message</a> after converting provider MP3 to AAC-LC mono 44.1 kHz M4A with fast-start metadata. This requires <code>ffmpeg</code> and <code>ffprobe</code> in the gateway host PATH; conversion errors produce <code>LINE_AUDIO_CONVERSION_FAILED</code> and retain the text response. Duration comes from the converted file. The 30-minute share URL ends in <code>/audio.m4a</code>; public fetches revalidate it and serve <code>audio/mp4</code> with <code>nosniff</code> (older MP3 shares retain <code>audio/mpeg</code>); ordinary <code>any</code> shares remain image/PDF-only. Turning voice Off suppresses synthesis and pending delivery; already accepted sends cannot be recalled.</p><h2 id="dashboard-and-retention" tabindex="-1">Dashboard and retention <a class="header-anchor" href="#dashboard-and-retention" aria-label="Permalink to &quot;Dashboard and retention {#dashboard-and-retention}&quot;">​</a></h2><p>Dashboard managed Sessions follow the current runtime lifecycle: only Agent sessions used or Worker sessions admitted in this runtime make a parent row visible. Stored conversations remain available through history; reading the dashboard does not activate them.</p><p><code>GET /status</code> retains legacy sessions and includes managed sessions in <code>agents[].sessions</code>, marked <code>orchestration: true</code>. Managed rows carry <code>status</code> (<code>thinking</code>, <code>working</code>, <code>queued</code>, <code>waiting_input</code>, <code>needs_reconciliation</code> or <code>idle</code>), nested <code>tasks</code> and <code>workerIds</code>. <code>agents[].orchestration</code> includes <code>agentProcesses</code>, task summaries and <code>workerPool</code> (<code>maxWorkers</code>, <code>idleTtlMs</code>, <code>workers</code>). Worker records expose worker/session/workstream IDs, busy/idle state and idle <code>expiresAt</code>. Task summaries expose predecessor, worker session, resume status, active host PID and latest tool. A container PID here is the host Docker client PID. Finished tasks do not claim a stale PID is still running.</p><p><code>/dashboard</code> displays managed and legacy sessions together; expanding a managed row reveals its worker/task details. Headless sessions have no interactive PTY viewer. The direct path is <code>/dashboard</code>; <code>/gateway/dashboard</code> requires a reverse-proxy prefix.</p><p>Worker idle expiry (default 10 minutes) retires a pool slot, not the task. Task records in the per-agent <code>orchestration.db</code> have <strong>no automatic deletion policy yet</strong>. Event/tool activity defaults to seven-day retention. Eligible private task workspaces are archived separately after seven days by default; host/container user files are excluded. Existing chat-history retention does not delete task records.</p>`,43)])])}const u=s(n,[["render",o]]);export{k as __pageData,u as default};
