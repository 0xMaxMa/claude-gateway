import{_ as a,o as t,c as s,a2 as i}from"./chunks/framework.D1JEzK2q.js";const g=JSON.parse('{"title":"Gateway settings","description":"","frontmatter":{},"headers":[],"relativePath":"reference/gateway-settings.md","filePath":"reference/gateway-settings.md"}'),n={name:"reference/gateway-settings.md"};function o(d,e,r,l,h,c){return t(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="gateway-settings" tabindex="-1">Gateway settings <a class="header-anchor" href="#gateway-settings" aria-label="Permalink to &quot;Gateway settings&quot;">​</a></h1><p>These settings are read from <code>config.json</code>. Examples are partial objects to merge into an existing configuration. Defaults in the tables refer to runtime fallback values; the installed template may explicitly choose a different value.</p><h2 id="gateway-timezone-optional" tabindex="-1"><code>gateway.timezone</code> (optional) <a class="header-anchor" href="#gateway-timezone-optional" aria-label="Permalink to &quot;\`gateway.timezone\` (optional)&quot;">​</a></h2><p>IANA timezone, default <code>&quot;UTC&quot;</code>. Shared default for the per-feature scheduling timezones below when they are unset or invalid: <code>gateway.history.cleanupTimezone</code>, <code>gateway.appBackup.cleanupTimezone</code>, <code>gateway.skillLearning.pruneTimezone</code>, <code>gateway.dreaming.dreamTimezone</code>, and <code>gateway.knowledge.reflection.timezone</code>. A valid per-feature field still overrides this shared default for that one feature; an invalid per-feature value falls through to <code>gateway.timezone</code> rather than being treated as set. An invalid <code>gateway.timezone</code> itself falls back to <code>&quot;UTC&quot;</code> rather than crashing that scheduler.</p><h2 id="gateway-publicurl-optional" tabindex="-1"><code>gateway.publicUrl</code> (optional) <a class="header-anchor" href="#gateway-publicurl-optional" aria-label="Permalink to &quot;\`gateway.publicUrl\` (optional)&quot;">​</a></h2><p>The externally reachable gateway base URL. Set it manually to enable short-lived public file shares used by <code>generate_image</code> reference edits and <code>share_file</code> (formerly <code>share_image</code>, which still works as a deprecated image-only alias). The URL must end in <code>/gateway</code>; changing it requires a gateway restart.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;gateway&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;publicUrl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://vm.example.com/gateway&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Created share URLs have the stable form <code>https://vm.example.com/gateway/shared/TOKEN</code>. When <code>publicUrl</code> is set the mint response includes this ready-built <code>url</code>; when it is unset the response still returns the <code>token</code> (the share endpoint stays enabled) and callers with their own public base — e.g. LINE, which derives its host from the inbound webhook — build <code>&lt;base&gt;/shared/&lt;token&gt;</code> themselves. HTTP is accepted only for local development hosts such as <code>http://host.docker.internal:10850/gateway</code>.</p><p>In orchestration tasks, pass the original absolute file path from the active task workspace, an input attachment, or the current session’s media directory to <code>share_file</code> or <code>task_stage_file</code>. The gateway copies authorized files into session media automatically. The old workflow of copying to the agent-wide <code>media/</code> root is outside worker scope. <code>ARTIFACT_PATH_DENIED</code> identifies a path outside that scope; use the authorized original path rather than retrying the same path.</p><h2 id="gateway-oauthreturnurl-optional" tabindex="-1"><code>gateway.oauthReturnUrl</code> (optional) <a class="header-anchor" href="#gateway-oauthreturnurl-optional" aria-label="Permalink to &quot;\`gateway.oauthReturnUrl\` (optional)&quot;">​</a></h2><p>Where to send the browser after a connector OAuth sign-in finishes. The gateway is product-agnostic and never hardcodes a downstream app&#39;s domain, so this is opt-in.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;gateway&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;oauthReturnUrl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://app.example.com/settings/connectors&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Set, the callback issues a real <code>302</code> to it on <strong>every</strong> terminal outcome — success, and also a denied, expired or failed sign-in, which carries <code>?connector_oauth_error=&lt;code&gt;</code>. Unset, the callback renders a plain &quot;Connected — you can close this tab&quot; page instead. The value is validated once at startup: anything that isn&#39;t a well-formed <code>http(s)</code> URL is logged and ignored rather than injecting a broken redirect into every future callback. The scheme is part of that check — this value becomes the <code>Location</code> of a redirect sent to the end user&#39;s own browser from a public route, so a <code>javascript:</code> or <code>data:</code> URL here would be script running on every sign-in, and is refused like any other malformed value.</p><h2 id="gateway-customconnectors-optional" tabindex="-1"><code>gateway.customConnectors</code> (optional) <a class="header-anchor" href="#gateway-customconnectors-optional" aria-label="Permalink to &quot;\`gateway.customConnectors\` (optional)&quot;">​</a></h2><p>User-pasted MCP connectors, keyed by a slugified id. Normally written through the API (<code>POST /api/v1/connectors/custom</code>) rather than by hand.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;gateway&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;customConnectors&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;firecrawl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;label&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Firecrawl&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;config&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">          &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;streamable-http&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">          &quot;url&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;https://mcp.firecrawl.dev/v2/mcp-oauth&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">          &quot;headers&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;Authorization&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Bearer {access_token}&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        },</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;secretNames&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;access_token&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">],</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">        &quot;credentialOwner&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;gateway&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">      }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Each entry is raw <code>mcpServers</code>-entry JSON with <code>{placeholder}</code> tokens standing in for secrets. <code>credentialOwner</code> records who holds the credential and keeps it valid — <code>none</code>, <code>static</code> (a pasted value), <code>gateway</code> (this gateway ran the OAuth flow and refreshes the token itself) or <code>external</code> (a control plane pushes tokens in). It is written by the route that creates the entry; see <a href="./../api/connectors.html">Connectors API</a>. <strong>Only the placeholder names are stored here</strong> — the values live in <code>~/.claude-gateway/mcp-token.env</code> (mode <code>0600</code>), namespaced <code>CUSTOM__&lt;connectorId&gt;__&lt;placeholderName&gt;</code>, and are substituted in when a session spawns. Override that file&#39;s path with <code>GATEWAY_MCP_TOKEN_ENV_PATH</code>.</p><p>Custom connectors are <strong>admin-trusted but not code-reviewed</strong> — the config is whatever the admin pasted. Per-agent enablement lives on the agent (<code>PATCH /api/v1/agents/:id</code> with <code>connectors</code>). Set an entry&#39;s <code>defaultEnabled: false</code> to require explicit per-agent opt-in, for example when pairing a personal browser. An omitted value preserves the gateway default; <code>true</code> does not override a gateway-wide opt-in policy. See <a href="./../api/connectors.html">Connectors API</a> for the full model, the OAuth flow, and the refresh behaviour.</p><h2 id="gateway-connectorsdefaultenabled-optional" tabindex="-1"><code>gateway.connectorsDefaultEnabled</code> (optional) <a class="header-anchor" href="#gateway-connectorsdefaultenabled-optional" aria-label="Permalink to &quot;\`gateway.connectorsDefaultEnabled\` (optional)&quot;">​</a></h2><p>Whether a connected connector is available to an agent that has no explicit entry in its own <code>connectors</code> map. Defaults to <code>true</code> — opt-out: connecting a connector makes it available everywhere, and an agent only misses it if explicitly disabled.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;gateway&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;connectorsDefaultEnabled&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Set it to <code>false</code> on a gateway that hosts agents for <strong>more than one person</strong>. The default suits the common single-operator install, but with several owners it hands a credential connected by one of them to every agent on the box — including agents whose chat users are not that person. With <code>false</code>, each agent has to be opted in explicitly (<code>PATCH /api/v1/agents/:id</code> with <code>{&quot;connectors&quot;: {&quot;&lt;id&gt;&quot;: {&quot;enabled&quot;: true}}}</code>).</p><p>Changing this affects the next session spawn, like any other connector change.</p><h2 id="gateway-logs-optional" tabindex="-1"><code>gateway.logs</code> (optional) <a class="header-anchor" href="#gateway-logs-optional" aria-label="Permalink to &quot;\`gateway.logs\` (optional)&quot;">​</a></h2><p>Verbosity, rotation and retention for the files in <code>logDir</code>. The whole block is optional — omit it and the defaults below apply.</p><table tabindex="0"><thead><tr><th>Field</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>level</code></td><td><code>&quot;info&quot;</code></td><td>Minimum level written, to both the file and stdout. One of <code>debug</code>, <code>info</code>, <code>warn</code>, <code>error</code></td></tr><tr><td><code>maxFileBytes</code></td><td><code>16777216</code> (16 MiB)</td><td>Rotate <code>&lt;name&gt;.log</code> to <code>&lt;name&gt;.log.1</code> once an append would carry it past this size</td></tr><tr><td><code>maxFiles</code></td><td><code>3</code></td><td>Rotated generations kept per stream; the oldest is deleted. Lowering it collects the generations it orphans at the next rotation. <code>0</code> = keep none</td></tr><tr><td><code>retentionDays</code></td><td><code>14</code></td><td>Delete logs (live and rotated) older than this, at boot and once a day. <code>0</code> = keep forever</td></tr></tbody></table><p><code>level</code> is the one that governs disk usage. Session processes log every stream event at <code>debug</code>, so <code>debug</code> is off by default. Set <code>&quot;level&quot;: &quot;debug&quot;</code> when you are actually chasing something, and expect the directory to grow quickly while it is on. Rotation and retention bound what is <em>kept</em>; only the level bounds what is <em>written</em>.</p><p>Retention is age-based because each session writes its own <code>&lt;agent&gt;:session:&lt;uuid&gt;.log</code> and never returns to it — <code>maxFiles</code> prunes generations of one stream, so it can never reach them.</p><p>This block is <strong>hot-reloaded</strong>: edit it in <code>config.json</code> and it applies on the next config reload, no restart. That matters because turning the level up is something you do while chasing a live problem, and a restart would kill the sessions you are trying to observe.</p><h2 id="session" tabindex="-1"><code>session</code> <a class="header-anchor" href="#session" aria-label="Permalink to &quot;\`session\`&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Field</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>idleTimeoutMinutes</code></td><td><code>30</code></td><td>Kill idle session subprocess after N minutes of inactivity. Inactivity means no incoming message <strong>and</strong> no subprocess output — a session actively producing output (e.g. a self-paced <code>/loop</code>) is not treated as idle</td></tr><tr><td><code>maxConcurrent</code></td><td><code>20</code></td><td>Max simultaneous active sessions per agent; oldest idle is evicted when exceeded</td></tr></tbody></table><h2 id="gateway-history-optional" tabindex="-1"><code>gateway.history</code> (optional) <a class="header-anchor" href="#gateway-history-optional" aria-label="Permalink to &quot;\`gateway.history\` (optional)&quot;">​</a></h2><p>Global default retention policy. Can be overridden per-agent with an <code>history</code> key inside the agent config.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;gateway&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;history&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;retentionDays&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">90</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;maxHistoryMessages&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;cleanupHour&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;cleanupTimezone&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Asia/Bangkok&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><table tabindex="0"><thead><tr><th>Field</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>retentionDays</code></td><td><code>null</code> (keep forever)</td><td>Delete messages older than N days on each cleanup cycle</td></tr><tr><td><code>maxHistoryMessages</code></td><td><code>50</code></td><td>Max history messages re-injected into a session at spawn. Lower it to shrink the context loaded at session start. <code>0</code> = inject no history</td></tr><tr><td><code>cleanupHour</code></td><td><code>3</code></td><td>Hour of day to run cleanup (24h, in <code>cleanupTimezone</code>)</td></tr><tr><td><code>cleanupTimezone</code></td><td><code>&quot;UTC&quot;</code></td><td>IANA timezone for the cleanup schedule; falls back to <code>gateway.timezone</code> when unset or invalid</td></tr></tbody></table><p>Per-agent override example:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;agents&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: [</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;id&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;alfred&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;history&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: { </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;retentionDays&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;maxHistoryMessages&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">30</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  ]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h2 id="dmpolicy" tabindex="-1"><code>dmPolicy</code> <a class="header-anchor" href="#dmpolicy" aria-label="Permalink to &quot;\`dmPolicy\`&quot;">​</a></h2><p>Access policy is configured per-channel in the agent&#39;s workspace state file, not in <code>config.json</code>:</p><table tabindex="0"><thead><tr><th>File</th><th>Path</th></tr></thead><tbody><tr><td>Telegram</td><td><code>~/.claude-gateway/agents/&lt;id&gt;/workspace/.telegram-state/access.json</code></td></tr><tr><td>Discord</td><td><code>~/.claude-gateway/agents/&lt;id&gt;/workspace/.discord-state/access.json</code></td></tr></tbody></table><table tabindex="0"><thead><tr><th>Value</th><th>Behaviour</th></tr></thead><tbody><tr><td><code>allowlist</code></td><td>Only user IDs in <code>allowFrom</code> can DM the agent (<strong>default</strong>)</td></tr><tr><td><code>open</code></td><td>Anyone can DM the agent</td></tr><tr><td><code>pairing</code></td><td>New users DM the bot to receive a pairing code; approve with <code>claude-gateway channels approve</code></td></tr></tbody></table><h2 id="voice-and-orchestration" tabindex="-1">Voice and orchestration <a class="header-anchor" href="#voice-and-orchestration" aria-label="Permalink to &quot;Voice and orchestration&quot;">​</a></h2><p>See <a href="./../guide/voice.html">voice setup</a>, <a href="./../guide/orchestration.html">orchestration behavior</a>, and <a href="./orchestration-settings.html">orchestration settings</a> for the per-agent configuration.</p><h2 id="gateway-headless" tabindex="-1"><code>gateway.headless</code> <a class="header-anchor" href="#gateway-headless" aria-label="Permalink to &quot;\`gateway.headless\`&quot;">​</a></h2><p>Controls the Claude subprocess backend for all non-app agents.</p><table tabindex="0"><thead><tr><th>Value</th><th>Backend</th><th>Description</th></tr></thead><tbody><tr><td><code>true</code> <em>(default)</em></td><td>Headless (<code>--print</code>)</td><td>Headless stream-JSON process; orchestration workers can resume saved CLI sessions</td></tr><tr><td><code>false</code></td><td>PTY shell wrapper</td><td>Interactive pseudo-terminal — full TUI support</td></tr></tbody></table><p><strong>Legacy app-agents always run headless</strong> regardless of this setting. Enabling orchestration automatically sets and saves <code>gateway.headless: true</code>. Disabling orchestration does not reset it.</p><p><code>--dangerously-skip-permissions</code> is always injected by the gateway automatically — there is no per-agent config field for it.</p><p>In PTY mode that flag makes Claude Code open a &quot;Bypass Permissions mode&quot; confirmation dialog at startup, which the wrapper accepts on your behalf. How it is accepted depends on the Claude Code build: releases up to <strong>2.1.247</strong> render numbered options (<code>1. No, exit</code> / <code>2. Yes, I accept</code>) and are accepted with the digit, while <strong>2.1.248 and newer</strong> drop the numbers, so the wrapper walks the caret onto the accept row and only then presses Enter. If a future release changes the dialog beyond what the wrapper recognises, it deliberately sends <strong>no</strong> keystroke and leaves the dialog on screen rather than risk selecting &quot;No, exit&quot; (which would exit Claude Code) — set <code>PTY_SHELL_SKIP_DIALOG_DISMISS=1</code> to turn the auto-accept off entirely.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;gateway&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;headless&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">false</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>This setting is hot-reloadable — new sessions pick it up without a restart.</p><h2 id="gateway-selfhealing-autorecover" tabindex="-1"><code>gateway.selfHealing.autoRecover</code> <a class="header-anchor" href="#gateway-selfhealing-autorecover" aria-label="Permalink to &quot;\`gateway.selfHealing.autoRecover\`&quot;">​</a></h2><p>Opt-in self-healing for the turn-trace watchdog (Epic #195). When a turn stalls, the gateway always detects it, logs a scrubbed incident, and notifies the affected chat. This flag additionally controls whether the gateway may <em>act</em> on a stall.</p><table tabindex="0"><thead><tr><th>Value</th><th>Behaviour</th></tr></thead><tbody><tr><td><code>false</code> <em>(default)</em></td><td>Detection + incident logging + notification only — no automatic action</td></tr><tr><td><code>true</code></td><td>The watchdog may run a whitelisted recovery for a stalled turn: a keystroke into the TUI (esc / enter / arrow / menu selection), a session restart, a reversible safe-mode fallback to the headless backend, and — after a successful unblock — a guarded resend of the last message (only if the turn produced no output, so it is never double-submitted)</td></tr></tbody></table><p>Recovery actions are clamped to a per-stage whitelist and a per-turn budget, and any local triage treats the on-screen text as untrusted data validated against a closed schema. Safe-mode auto-fallback on a hard PTY failure is independent of this flag (it is always reversible and never presses keys). In-memory only — a gateway restart re-reads your real config.</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;gateway&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;selfHealing&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;autoRecover&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h2 id="gateway-bind" tabindex="-1"><code>gateway.bind</code> <a class="header-anchor" href="#gateway-bind" aria-label="Permalink to &quot;\`gateway.bind\`&quot;">​</a></h2><p>Network interface the HTTP/WebSocket server binds to. Defaults to <code>127.0.0.1</code> (localhost-only), so the dashboard and API are <strong>not</strong> exposed to the local network out of the box. Set to <code>0.0.0.0</code> to listen on all interfaces (for example when a containerized reverse proxy needs to reach the gateway). The <code>GATEWAY_BIND</code> environment variable, when set, takes precedence over this field.</p><blockquote><p><strong>⚠️ Binding to <code>0.0.0.0</code>? Configure an admin key in <code>gateway.api.keys</code>.</strong> The monitoring surface (<code>/status</code>, <code>/processes</code>) and the dashboard require an <strong>admin</strong> API key (<code>admin: true</code>) or a dashboard session when keys are configured — a scoped or write-only key is rejected (<code>401</code>), because the dashboard grants cross-agent, host-wide power (including PTY keystroke injection into any session). The dashboard prompts for an admin key at <code>/dashboard</code> and stores an <code>HttpOnly</code> session cookie (issued only to an admin key). <code>/health</code> stays public but returns only <code>{&quot;status&quot;:&quot;ok&quot;}</code> (no agent ids). With <strong>no</strong> keys configured the gateway <strong>fails closed on a non-loopback bind</strong>: <code>/status</code>, <code>/processes</code>, and <code>/dashboard</code> return <code>503</code> until you set <code>gateway.api.keys</code> (a startup warning is logged); if keys are set but <strong>none is admin</strong>, the dashboard is inaccessible and a startup warning is logged. On a loopback bind they stay open, so local keyless installs are unaffected. The gateway serves plain HTTP; put TLS in front (reverse proxy) so credentials are not sent in the clear.</p></blockquote><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;gateway&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;bind&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;127.0.0.1&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><blockquote><p><strong>⚠️ Upgrade note:</strong> the default bind changed from <code>0.0.0.0</code> to <code>127.0.0.1</code> (configVersion 1.0.13). To avoid silently cutting off external access, the config migrator is <strong>behavior-preserving</strong>: whenever it upgrades a config that never set <code>gateway.bind</code>, it pins <code>bind</code> to <code>0.0.0.0</code> and logs a one-time warning, so a deployment that was reachable from another host stays reachable. This applies to <em>any</em> upgraded config with no <code>bind</code> key — including one already stamped <code>1.0.13</code> that never received a bind (an earlier version gated this on <code>&lt; 1.0.13</code> and left such configs stuck on the <code>127.0.0.1</code> default). New installs (no prior config, so no migration runs) keep the secure <code>127.0.0.1</code> default. If you <em>want</em> localhost-only after upgrading, set <code>gateway.bind</code> to <code>127.0.0.1</code> explicitly (or the <code>GATEWAY_BIND</code> env var).</p></blockquote><h2 id="terminal-viewer-—-interactive-terminal-mode" tabindex="-1">Terminal Viewer — interactive terminal mode <a class="header-anchor" href="#terminal-viewer-—-interactive-terminal-mode" aria-label="Permalink to &quot;Terminal Viewer — interactive terminal mode&quot;">​</a></h2><p>The dashboard&#39;s <strong>Terminal Viewer</strong> opens read-only (a live mirror of the PTY). A toggle in the top-right of the viewer switches it into an <strong>interactive terminal</strong>: keystrokes typed into the panel — printable characters, Enter, arrows, Ctrl-combos, Esc — are streamed into the live PTY, and the panel title changes to reflect the active mode. This is a per-browser client-side choice (Issue #201); there is no server config flag to enable it.</p><p>Because interactive mode turns a read-only view into a remote-write surface, access is protected upstream rather than by a feature flag:</p><ul><li><strong>Authentication</strong> — when API keys are configured, the WebSocket requires a valid dashboard ticket or <strong>admin</strong> API key. The ticket is minted at <code>POST /api/v1/pty-stream-ticket</code>, which itself requires an admin API key or a valid dashboard session cookie — so an unauthenticated or non-admin caller cannot obtain one in keyed deployments. Keyless loopback installations retain local monitoring access; do not treat loopback reachability as per-user authentication. The dashboard gets its session by logging in with an admin key at <code>/dashboard</code> (<code>HttpOnly</code> cookie); no token is embedded in the page.</li><li><strong><code>gateway.bind</code></strong> — the gateway binds to <code>127.0.0.1</code> (localhost) by default, so the dashboard is not reachable from the network out of the box. On a non-loopback bind (<code>0.0.0.0</code>), configure an admin key in <code>gateway.api.keys</code> so the dashboard and monitoring endpoints require an admin credential, and prefer a TLS-terminating reverse proxy so credentials are not sent in the clear.</li></ul><p>Inbound frames are always bounded (text-only, size-capped) and are dropped for headless sessions (no PTY).</p><h4 id="cli-—-open-the-terminal-viewer-from-chat" tabindex="-1"><code>/cli</code> — open the terminal viewer from chat <a class="header-anchor" href="#cli-—-open-the-terminal-viewer-from-chat" aria-label="Permalink to &quot;\`/cli\` — open the terminal viewer from chat&quot;">​</a></h4><p>The <code>/cli</code> command (Telegram, Discord, LINE) opens the same live terminal viewer for <strong>one agent</strong>, without an admin key. It requires <code>gateway.publicUrl</code> and an agent running with <code>gateway.headless: false</code>. Unlike the admin dashboard, a <code>/cli</code> session is <strong>agent-scoped</strong>: its cookie and PTY ticket can only reach the originating agent&#39;s own sessions — never another agent, the process tree, or a cross-agent stream.</p><p>The viewer link is never a credential; unlocking it requires a proof tied to an allowlist-gated chat action:</p><ul><li><strong>Telegram</strong> opens a Mini App and the gateway verifies Telegram&#39;s signed <code>initData</code> (HMAC with the agent&#39;s own bot token) — nothing secret rides in the URL, and the <code>initData</code> user must match the user who ran <code>/cli</code>.</li><li><strong>Discord</strong> and <strong>LINE</strong> send an open-viewer link plus an <strong>Approve</strong> button; the browser stays locked until you approve in the chat, so a leaked or forwarded link cannot be unlocked by anyone who cannot approve there.</li></ul><p>The first browser to open a link owns it (opening the link in a second browser is rejected), the viewer defaults to read-only (toggle for input), and viewer sessions expire (30 min) — send <code>/cli</code> again to reconnect.</p><h2 id="gateway-api-keys" tabindex="-1"><code>gateway.api.keys</code> <a class="header-anchor" href="#gateway-api-keys" aria-label="Permalink to &quot;\`gateway.api.keys\`&quot;">​</a></h2><p>Each key has a <code>key</code> string (supports <code>\${ENV_VAR}</code> interpolation), an optional <code>description</code>, and an <code>agents</code> field — either an array of agent IDs or <code>&quot;*&quot;</code> for all-agent scope. Administrator access separately requires <code>admin: true</code>; all-agent scope alone is not an administrator grant. Keys support both <code>Authorization: Bearer</code> and <code>X-Api-Key</code> headers.</p><h2 id="bot-tokens" tabindex="-1">Bot tokens <a class="header-anchor" href="#bot-tokens" aria-label="Permalink to &quot;Bot tokens&quot;">​</a></h2><p>Tokens are stored per-agent at <code>~/.claude-gateway/agents/&lt;id&gt;/.env</code> and auto-loaded at startup <strong>and before every config reload</strong> — so an agent added to <code>config.json</code> while the gateway is running starts without a restart, even though its token only exists in a brand-new <code>.env</code>. Use <code>\${AGENT_BOT_TOKEN}</code> syntax in config to reference them, or set them as shell environment variables. Lines are <code>KEY=value</code>; <code>#</code> comments and blank lines are ignored, and surrounding quotes are stripped, the same as in <code>~/.claude-gateway/.env</code>.</p><p>A variable you exported yourself always wins over the <code>.env</code> file and is never replaced by a reload. A token the gateway did read from a <code>.env</code> is refreshed when that file changes, so <strong>rotating a token takes effect on the next config reload</strong> rather than at the next restart. Note that only <code>config.json</code> is watched — editing a <code>.env</code> by hand applies on the following reload, while the MCP <code>agent_create</code> / <code>agent_update</code> tools write both files and so take effect immediately. If a <code>\${VAR}</code> cannot be resolved from anywhere, that one agent is skipped — the rest of the gateway starts normally — and the skip is logged to <code>logs/gateway.log</code> with the name of the missing variable.</p><p>WhatsApp Cloud&#39;s credentials (<code>accessToken</code>, <code>phoneNumberId</code>, <code>appSecret</code>, <code>verifyToken</code>) are plain fields under the agent&#39;s <code>whatsapp_cloud</code> config block, not a dedicated &quot;bot token&quot; field — but they resolve through the exact same mechanism as Telegram/Discord bot tokens: reference them as <code>\${VAR}</code> in <code>config.json</code> and put the value in the agent&#39;s <code>.env</code> (or export it as a shell variable), same as above.</p><hr><p>Implementation: <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/config/loader.ts" target="_blank" rel="noreferrer">config loader</a>, <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/config.template.json" target="_blank" rel="noreferrer">configuration template</a>.</p>`,79)])])}const k=a(n,[["render",o]]);export{g as __pageData,k as default};
