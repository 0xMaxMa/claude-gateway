import{_ as t,o as a,c as r,a2 as o}from"./chunks/framework.D1JEzK2q.js";const u=JSON.parse('{"title":"Architecture","description":"","frontmatter":{},"headers":[],"relativePath":"reference/architecture.md","filePath":"reference/architecture.md"}'),s={name:"reference/architecture.md"};function n(i,e,d,c,l,h){return a(),r("div",null,[...e[0]||(e[0]=[o('<h1 id="architecture" tabindex="-1">Architecture <a class="header-anchor" href="#architecture" aria-label="Permalink to &quot;Architecture&quot;">​</a></h1><p>Claude Gateway coordinates Claude Code rather than replacing it with a separate model runtime. The gateway owns channel ingress, access checks, conversation state, tasks, provider configuration, speech transport and delivery. Claude Code supplies the agent and worker execution environment.</p><h2 id="from-a-message-to-a-result" tabindex="-1">From a message to a result <a class="header-anchor" href="#from-a-message-to-a-result" aria-label="Permalink to &quot;From a message to a result&quot;">​</a></h2><ol><li>A channel receiver or authenticated API accepts input. Channel identity and API principal checks decide which agent and conversation it can access.</li><li>The orchestration ingress persists the input. Repeated delivery IDs can be recognized instead of starting duplicate work.</li><li>The conversation agent reads the input and relevant current state. A complete work instruction receives a contextual acknowledgement; a direct question can receive a direct answer.</li><li>Substantial work is admitted as a task with a worker profile, revision, dependencies and workspace policy.</li><li>The scheduler starts or reuses a compatible Claude Code worker. Tool calls and progress update durable task activity.</li><li>Worker completion or a request for input returns to the conversation through events. The agent reports the result or asks the needed question.</li><li>Channel delivery formats text for that platform and, if enabled for the input, generates a spoken response. Browser clients also receive session activity and audio streams.</li></ol><p>The conversation agent can explain progress while a worker runs. A worker does not need to finish before another user message is accepted. A successful HTTP admission response proves acceptance, not completion of a task or outbound message.</p><h2 id="processes-and-ownership" tabindex="-1">Processes and ownership <a class="header-anchor" href="#processes-and-ownership" aria-label="Permalink to &quot;Processes and ownership&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Component</th><th>Responsibility</th><th>Lifetime</th></tr></thead><tbody><tr><td>Gateway</td><td>Configuration, API, scheduling, access, delivery, state</td><td>Service/foreground process</td></tr><tr><td>Channel receiver</td><td>Platform connection and incoming updates</td><td>Owned by the gateway; implementation varies by channel</td></tr><tr><td>Conversation decision</td><td>Interpret current input/events and choose the next response/actions</td><td>Headless Claude Code turn with bounded decision budgets</td></tr><tr><td>Worker</td><td>Execute an admitted task with the allowed tools and workspace</td><td>Reusable session, bounded by pool policy and idle TTL</td></tr><tr><td>MCP server</td><td>Expose gateway tools according to the agent/worker profile</td><td>Tied to the Claude Code execution context</td></tr><tr><td>External connector</td><td>Third-party MCP endpoint selected and authorized by the operator</td><td>Managed separately; injected into authorized execution contexts</td></tr></tbody></table><p><code>gateway.orchestration: false</code> selects the legacy conversation path. <code>gateway.headless: false</code> is the optional legacy PTY backend. Orchestration requires headless execution and Linux supervision; it is not a PTY session hidden behind the web UI.</p><h2 id="what-persists" tabindex="-1">What persists <a class="header-anchor" href="#what-persists" aria-label="Permalink to &quot;What persists&quot;">​</a></h2><table tabindex="0"><thead><tr><th>State</th><th>Purpose</th></tr></thead><tbody><tr><td>Agent workspace source Markdown</td><td>Identity, rules, preferences, core memory, skills</td></tr><tr><td>Generated Claude instructions</td><td>Composed view read by the subprocess; edit source files instead</td></tr><tr><td>Session context/history</td><td>Conversation continuity and searchable past messages</td></tr><tr><td><code>orchestration.db</code> per agent</td><td>Inputs, decisions, tasks, revisions, events, worker state and deliveries</td></tr><tr><td>Memory archive / shared knowledge</td><td>Searchable longer-lived information outside the core prompt budget</td></tr><tr><td>Replay audio</td><td>Completed retained speech, subject to size/age budgets</td></tr><tr><td>App data and backups</td><td>Docker app state, separate from chat history</td></tr></tbody></table><p>A process restart and deletion of persistent state are different operations. Cancellation does not roll back external side effects or erase work files. After an interrupted run, reconcile actual files and external state before retrying.</p><h2 id="tools-and-workspace-boundaries" tabindex="-1">Tools and workspace boundaries <a class="header-anchor" href="#tools-and-workspace-boundaries" aria-label="Permalink to &quot;Tools and workspace boundaries&quot;">​</a></h2><p>Worker profile admission checks the exposed tool inventory. Gateway and MCP code must be from a consistent deployment. A profile mismatch is an actionable configuration/deployment failure; it must not be bypassed by granting every tool.</p><p>Host workers use the gateway&#39;s OS account and authorized tools. Default host work requires no Git repository. <code>isolated-worktree</code> is explicit, and provides a separate Git working tree rather than an operating-system sandbox. Installed app-agents and their workers execute inside the app&#39;s validated container; they must not inherit host shell/MCP access, the host Docker socket, or a fallback host worker.</p><p>See <a href="./../guide/tools.html">tools and connectors</a>, <a href="./orchestration-settings.html">worker settings</a>, and <a href="./../guide/apps.html">app boundaries</a>.</p><h2 id="voice-is-a-separate-delivery-path" tabindex="-1">Voice is a separate delivery path <a class="header-anchor" href="#voice-is-a-separate-delivery-path" aria-label="Permalink to &quot;Voice is a separate delivery path&quot;">​</a></h2><p>Speech recognition produces text input. The conversation agent produces display text and approved spoken text. Speech synthesis converts that spoken text into audio, which is delivered after or alongside the appropriate text protocol. The voice language follows the response language; a chosen voice does not translate the answer.</p><p>Provider behavior affects latency: realtime STT can emit partial words; recorded STT waits for the completed segment. Some TTS adapters stream audio while others buffer provider output. Browser replay uses a retained recording, not another model request. See <a href="./../guide/voice.html">voice</a> and its <a href="./../api/voice.html">protocol</a>.</p><p>Implementation: <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/orchestration/runtime.ts" target="_blank" rel="noreferrer">orchestration runtime</a>, <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/agent/runner.ts" target="_blank" rel="noreferrer">agent runner</a>, <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/mcp/server.ts" target="_blank" rel="noreferrer">MCP server</a>, <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/voice/session.ts" target="_blank" rel="noreferrer">voice session</a>.</p>',19)])])}const b=t(s,[["render",n]]);export{u as __pageData,b as default};
