import{_ as s,o as t,c as a,a2 as i}from"./chunks/framework.D1JEzK2q.js";const k=JSON.parse('{"title":"Public Webhook Ingress","description":"","frontmatter":{},"headers":[],"relativePath":"api/webhooks.md","filePath":"api/webhooks.md"}'),n={name:"api/webhooks.md"};function o(d,e,h,l,p,r){return t(),a("div",null,[...e[0]||(e[0]=[i(`<h1 id="public-webhook-ingress" tabindex="-1">Public Webhook Ingress <a class="header-anchor" href="#public-webhook-ingress" aria-label="Permalink to &quot;Public Webhook Ingress {#public-webhook-ingress}&quot;">​</a></h1><p>For channel setup, see <a href="/claude-gateway/channels/line.html">LINE</a>, <a href="/claude-gateway/channels/slack.html">Slack</a> and <a href="/claude-gateway/channels/whatsapp-cloud.html">WhatsApp Cloud</a>. The provider dispatch also accepts <code>app: &quot;whatsapp_cloud&quot;</code>; its GET verification and signed POST deliveries use the same <code>/webhooks/:app/:agentId</code> surface.</p><h2 id="line-pending-senders" tabindex="-1">LINE pending senders <a class="header-anchor" href="#line-pending-senders" aria-label="Permalink to &quot;LINE pending senders&quot;">​</a></h2><h3 id="get-api-v1-agents-agentid-line-pending" tabindex="-1">GET /api/v1/agents/:agentId/line/pending <a class="header-anchor" href="#get-api-v1-agents-agentid-line-pending" aria-label="Permalink to &quot;GET /api/v1/agents/:agentId/line/pending&quot;">​</a></h3><p>Requires an admin API key. Returns <code>{ &quot;senders&quot;: [] }</code> with up to five recently denied senders for this agent&#39;s LINE channel. Each entry has <code>userId</code>, <code>firstSeen</code>, <code>lastSeen</code> (Unix milliseconds) and <code>count</code>; optional fields are <code>displayName</code>, <code>kind</code> (<code>user</code>, <code>group</code> or <code>room</code>) and a pairing <code>code</code>. This list is held in memory and clears on restart. A repeated sender updates its count and timestamp; older entries are evicted when the cap is exceeded.</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --fail</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://127.0.0.1:10850/api/v1/agents/assistant/line/pending</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;X-Api-Key: </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$CLAUDE_GATEWAY_API_KEY</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span></code></pre></div><h3 id="delete-api-v1-agents-agentid-line-pending-senderid" tabindex="-1">DELETE /api/v1/agents/:agentId/line/pending/:senderId <a class="header-anchor" href="#delete-api-v1-agents-agentid-line-pending-senderid" aria-label="Permalink to &quot;DELETE /api/v1/agents/:agentId/line/pending/:senderId&quot;">​</a></h3><p>Requires an admin API key. Dismiss a pending LINE user, group or room identifier. Returns <code>{ &quot;ok&quot;: true }</code>, including when the identifier is already absent. This does not add the sender to an allowlist; another denied message may add it again. Use the agent PATCH API to persist an access policy change.</p><p>Both routes return <code>403</code> for a non-admin key and <code>404</code> for an unknown agent, in addition to API authentication errors.</p><h2 id="provider-verification-and-delivery" tabindex="-1">Provider verification and delivery <a class="header-anchor" href="#provider-verification-and-delivery" aria-label="Permalink to &quot;Provider verification and delivery&quot;">​</a></h2><p>All external, unauthenticated webhooks (LINE today; more apps can be added later) enter through a single dispatcher route <code>/webhooks/:app/:agentId?</code>, routed to a per-app handler by the <code>:app</code> path segment.</p><p><strong>This zone bypasses the gateway&#39;s API-key auth entirely</strong> — it is mounted outside the <code>/api</code> routers, before <code>express.json()</code>, so each handler gets the raw request bytes it needs for its own signature validation. There is no ambient auth: every app handler (e.g. LINE&#39;s <code>x-line-signature</code> HMAC check) <strong>must authenticate its own requests</strong> as its first step.</p><p>Request bodies on this route are capped at 256KB (pre-auth cap, applies to every app). An unknown <code>:app</code> returns <code>404</code>:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://localhost:10850/webhooks/nope</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;error&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;unknown webhook app: nope&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><h2 id="line-app-line" tabindex="-1">LINE (<code>app: &quot;line&quot;</code>) <a class="header-anchor" href="#line-app-line" aria-label="Permalink to &quot;LINE (\`app: &quot;line&quot;\`) {#line-app-line}&quot;">​</a></h2><p><strong>Setup:</strong> configure <code>line.channelSecret</code> + <code>line.channelAccessToken</code> for an agent via <code>PATCH /api/v1/agents/:agentId</code> (see Agent API above), then point the LINE Developer Console&#39;s webhook URL at:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>https://&lt;your-gateway-host&gt;/webhooks/line/&lt;agentId&gt;</span></span></code></pre></div><p><code>&lt;agentId&gt;</code> may be omitted — the dispatcher then falls back to the first agent with <code>line.channelSecret</code> set — but an explicit ID is recommended once more than one agent has LINE configured.</p><p><strong>Verification (<code>GET</code>):</strong> LINE&#39;s Console &quot;Verify&quot; button sends a GET (or empty POST); the handler answers unconditionally:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;ok&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p><strong>Inbound delivery (<code>POST</code>):</strong> the request must carry a valid <code>x-line-signature</code> header (HMAC-SHA256 of the raw body, keyed by <code>channelSecret</code>, base64-encoded).</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">BODY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;{&quot;events&quot;:[{&quot;type&quot;:&quot;message&quot;,&quot;message&quot;:{&quot;type&quot;:&quot;text&quot;,&quot;id&quot;:&quot;1&quot;,&quot;text&quot;:&quot;hi&quot;},&quot;source&quot;:{&quot;type&quot;:&quot;user&quot;,&quot;userId&quot;:&quot;Uxxxx&quot;},&quot;replyToken&quot;:&quot;xxx&quot;,&quot;timestamp&quot;:1234567890}]}&#39;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SIG</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">printf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;%s&#39;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$BODY</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> openssl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dgst</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -sha256</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -hmac</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;&lt;CHANNEL_SECRET&gt;&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -binary</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> base64</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://localhost:10850/webhooks/line/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">agentI</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Content-Type: application/json&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;x-line-signature: </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$SIG</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$BODY</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span></code></pre></div><p>Text, image, and file messages from allowed 1:1/group/room sources are normalized and forwarded to the agent&#39;s <code>/channel</code> intake (the same path Telegram uses). Image and file bytes are fetched via the LINE blob API and handed to the agent as <code>meta.image_path</code>; a file also carries <code>meta.attachment_kind: &quot;document&quot;</code> and <code>meta.attachment_name</code> (the sanitized sender-supplied name). LINE sends no MIME type for a file, so the stored extension is derived from that name and constrained to a short alphanumeric run — anything unusable, or any type a browser would render as active content (the HTML and XML families, script and style), is stored as <code>.bin</code>. When the bytes cannot be fetched — over the 20 MB media cap, an empty upload, or a failed transfer — the turn is still forwarded, with no <code>meta.image_path</code> and a <code>content</code> that states the file is not available and why, so the agent never mistakes a lost attachment for one it has yet to open. Sticker, video, and location messages are ignored. Audio messages are transcribed when orchestration and the Agent’s voice-note recognition are enabled; legacy mode ignores audio. The request is acknowledged (<code>200 {&quot;ok&quot;:true}</code>) <strong>before</strong> event processing, so LINE never sees a slow response.</p><p><strong>Error responses:</strong></p><table tabindex="0"><thead><tr><th>Status</th><th>When</th></tr></thead><tbody><tr><td>401</td><td>Missing or invalid <code>x-line-signature</code> (the resolved agent always has <code>channelSecret</code> set — see the 404 row below)</td></tr><tr><td>404</td><td>No LINE-enabled agent found — no agent has <code>line.channelSecret</code> set, or the given <code>:agentId</code> doesn&#39;t</td></tr></tbody></table><p><strong>Access control:</strong> DMs and groups/rooms are closed by default (<code>dmPolicy</code> / <code>groupPolicy</code> allowlist, per agent config); a denied sender receives a one-time pairing code (via LINE reply) to share with the admin, who approves it the same way as Telegram pairing.</p><h2 id="slack-app-slack" tabindex="-1">Slack (<code>app: &quot;slack&quot;</code>) <a class="header-anchor" href="#slack-app-slack" aria-label="Permalink to &quot;Slack (\`app: &quot;slack&quot;\`) {#slack-app-slack}&quot;">​</a></h2><p><strong>Setup:</strong> configure <code>slack.botToken</code> (Bot User OAuth Token, <code>xoxb-…</code>) + <code>slack.signingSecret</code> for an agent via <code>PATCH /api/v1/agents/:agentId</code> (see Agent API above — both must be sent together, and the token is validated with <code>auth.test</code> at save time). Then, in the Slack app&#39;s <strong>Event Subscriptions</strong>, point the Request URL at:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>https://&lt;your-gateway-host&gt;/webhooks/slack/&lt;agentId&gt;</span></span></code></pre></div><p>Subscribe the bot to <code>message.im</code> (DMs) and <code>app_mention</code> (channel mentions). <strong>Socket Mode must be off</strong> — this is the HTTP Request URL integration, not Socket Mode.</p><p><code>&lt;agentId&gt;</code> may be omitted — the dispatcher then falls back to the first agent with <code>slack.signingSecret</code> set — but an explicit ID is recommended once more than one agent has Slack configured.</p><p><strong>URL verification (<code>POST</code>):</strong> Slack&#39;s one-time handshake arrives as a <em>signed</em> POST with <code>{&quot;type&quot;:&quot;url_verification&quot;,&quot;challenge&quot;:&quot;…&quot;}</code> (unlike LINE&#39;s unsigned Console button). The signature is verified like any other request, then the raw <code>challenge</code> is echoed back:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;challenge&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;&lt;the value Slack sent&gt;&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p><strong>Inbound delivery (<code>POST</code>):</strong> the request must carry a valid <code>x-slack-signature</code> header — <code>v0=</code> + HMAC-SHA256 of <code>v0:{timestamp}:{rawBody}</code>, keyed by <code>signingSecret</code> — together with <code>x-slack-request-timestamp</code>. Requests whose timestamp is more than 5 minutes from now are rejected (replay protection).</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">TS</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">date</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> +%s</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">BODY</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;{&quot;type&quot;:&quot;event_callback&quot;,&quot;event_id&quot;:&quot;Ev1&quot;,&quot;authorizations&quot;:[{&quot;user_id&quot;:&quot;U0BOT&quot;}],&quot;event&quot;:{&quot;type&quot;:&quot;app_mention&quot;,&quot;channel&quot;:&quot;C123&quot;,&quot;user&quot;:&quot;U456&quot;,&quot;text&quot;:&quot;&lt;@U0BOT&gt; hi&quot;,&quot;ts&quot;:&quot;1.2&quot;,&quot;event_ts&quot;:&quot;1.2&quot;}}&#39;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">SIG</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;v0=$(</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">printf</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;v0:%s:%s&#39; &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$TS</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot; &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$BODY</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> openssl</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> dgst </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">-sha256</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -hmac</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;&lt;SIGNING_SECRET&gt;&quot; </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> sed</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;s/^.*</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> //&#39;)&quot;</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> POST</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> http://localhost:10850/webhooks/slack/</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">agentI</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">d</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Content-Type: application/json&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;x-slack-request-timestamp: </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$TS</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;x-slack-signature: </span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$SIG</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">$BODY</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;</span></span></code></pre></div><p>Text messages from allowed DMs and <code>@mention</code>s in allowed channels are normalized and forwarded to the agent&#39;s <code>/channel</code> intake (the same path Telegram/LINE use); the bot&#39;s own self-mention is stripped from <code>app_mention</code> text. The request is acknowledged (<code>200 {&quot;ok&quot;:true}</code>) <strong>before</strong> event processing, so Slack never sees a slow response and its 3-second-ack retry is avoided. Duplicate retries (Slack&#39;s at-least-once delivery) are de-duplicated by <code>event_id</code>.</p><p><strong>Error responses:</strong></p><table tabindex="0"><thead><tr><th>Status</th><th>When</th></tr></thead><tbody><tr><td>401</td><td>Missing or invalid <code>x-slack-signature</code> (or a timestamp outside the 5-minute window)</td></tr><tr><td>400</td><td>Body was not valid JSON</td></tr><tr><td>404</td><td>No Slack-enabled agent found — no agent has <code>slack.signingSecret</code> set, or the given <code>:agentId</code> doesn&#39;t</td></tr></tbody></table><p><strong>Access control:</strong> DMs (gated on the sender&#39;s Slack user id) and channels (gated on the channel id) are closed by default (<code>slack.dmPolicy</code> / <code>slack.groupPolicy</code> allowlist, per agent config); allowlist entries MUST be stable Slack ids (<code>U…</code> for users, <code>C…</code> for channels), never display/channel names. A denied sender receives a one-time pairing code (via <code>chat.postMessage</code>) to share with the admin, who approves it the same way as Telegram pairing. In channels, only <code>@mention</code>s are answered unless <code>slack.requireMention</code> is set to <code>false</code>.</p><p><strong>Pending-sender discovery (admin only):</strong> the recently-denied Slack senders/channels are surfaced for one-click allowlisting, mirroring LINE:</p><table tabindex="0"><thead><tr><th>Method</th><th>Endpoint</th><th>Auth</th></tr></thead><tbody><tr><td><code>GET</code></td><td><code>/api/v1/agents/:agentId/slack/pending</code></td><td>Admin key — list recently-denied Slack senders (id, best-effort name, pairing code)</td></tr><tr><td><code>DELETE</code></td><td><code>/api/v1/agents/:agentId/slack/pending/:senderId</code></td><td>Admin key — dismiss one knock from the in-memory pending list</td></tr></tbody></table><hr>`,43)])])}const g=s(n,[["render",o]]);export{k as __pageData,g as default};
