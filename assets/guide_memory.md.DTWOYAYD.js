import{_ as a,o as t,c as o,a2 as r}from"./chunks/framework.D1JEzK2q.js";const u=JSON.parse('{"title":"Memory and knowledge","description":"","frontmatter":{},"headers":[],"relativePath":"guide/memory.md","filePath":"guide/memory.md"}'),n={name:"guide/memory.md"};function d(i,e,s,c,l,h){return t(),o("div",null,[...e[0]||(e[0]=[r('<h1 id="memory-and-knowledge" tabindex="-1">Memory and knowledge <a class="header-anchor" href="#memory-and-knowledge" aria-label="Permalink to &quot;Memory and knowledge&quot;">​</a></h1><p>Use core memory for information an agent should see every turn, and the searchable archive for detail it can retrieve when needed.</p><table tabindex="0"><thead><tr><th>Layer</th><th>Use it for</th><th>How to inspect it</th></tr></thead><tbody><tr><td><code>MEMORY.md</code> and <code>USER.md</code></td><td>Durable facts and preferences</td><td>Read the workspace files</td></tr><tr><td>Per-agent archive</td><td>Longer notes outside the core budget</td><td>Ask for <code>memory_search</code> and <code>memory_get</code></td></tr><tr><td>Shared knowledge</td><td>Knowledge made available across agents</td><td>Dashboard Knowledge base tab</td></tr><tr><td>Chat history</td><td>Past conversations</td><td>Session history and history search</td></tr></tbody></table><h2 id="verify-recall" tabindex="-1">Verify recall <a class="header-anchor" href="#verify-recall" aria-label="Permalink to &quot;Verify recall&quot;">​</a></h2><p>Add a harmless project fact to a workspace note. Ask the agent to retrieve that specific fact and cite the source. Repeat in a new session to distinguish persistent recall from the current conversation&#39;s context. A plausible answer without a source is insufficient verification.</p><h2 id="control-the-budget" tabindex="-1">Control the budget <a class="header-anchor" href="#control-the-budget" aria-label="Permalink to &quot;Control the budget&quot;">​</a></h2><p>The template sets <code>gateway.memory.memoryBudgetChars</code> to <code>8000</code> and <code>userBudgetChars</code> to <code>3000</code>, with <code>overBudget: &quot;warn&quot;</code>. Archive notes that do not belong in the core context instead of increasing the budget indefinitely.</p><p><code>gateway.knowledge.archive</code> configures the SQLite/FTS5 searchable archive. <code>gateway.knowledge.shared</code> controls the shared knowledge location and mode. See the <a href="./../reference/configuration.html">configuration reference</a> before changing defaults.</p><h2 id="review-nightly-dreaming" tabindex="-1">Review nightly dreaming <a class="header-anchor" href="#review-nightly-dreaming" aria-label="Permalink to &quot;Review nightly dreaming&quot;">​</a></h2><p><code>gateway.dreaming</code> schedules background consolidation. The safe applier backs up memory and bounds changes; archived material can remain searchable after it leaves core memory. Inspect the dashboard&#39;s Nightly dreaming tab and the agent&#39;s <code>.dreaming/DREAMS.md</code> audit to understand a run.</p><p>Do not confuse history retention with memory consolidation. <code>gateway.history</code> and agent history overrides remove old chat/media records on their configured schedule; dreaming changes memory content.</p><p>Source: <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/agent/workspace-loader.ts" target="_blank" rel="noreferrer">workspace memory composition</a>.</p><h2 id="write-durable-context-and-episodic-notes-separately" tabindex="-1">Write durable context and episodic notes separately <a class="header-anchor" href="#write-durable-context-and-episodic-notes-separately" aria-label="Permalink to &quot;Write durable context and episodic notes separately&quot;">​</a></h2><p>Put standing preferences, recurring rules, and durable lessons in <code>MEMORY.md</code> or <code>USER.md</code>. Put dated work records in <code>memory/&lt;topic&gt;.md</code>, for example <code>memory/release-notes.md</code>. The archive indexes <code>MEMORY.md</code>, <code>USER.md</code>, and Markdown files recursively under <code>memory/</code>; an arbitrary project file outside those locations is not automatically a memory source.</p><p>With <code>gateway.memory.writeRouting: true</code>, generated instructions explicitly teach the agent this two-tier write contract. Its built-in default is <code>false</code>. The routing instruction guides the agent; it does not itself move existing content.</p><p>The per-agent search database is <code>&lt;workspace&gt;/../kb.sqlite</code>, next to <code>history.db</code>. Markdown remains the source material. Reindexing skips unchanged files by content hash, replaces chunks for changed files, and removes chunks whose source files disappeared. Default chunking uses 400 tokens with 80-token overlap and the SQLite FTS5 <code>unicode61</code> tokenizer.</p><p>Source: <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/agent/knowledge/indexer.ts" target="_blank" rel="noreferrer">archive indexer</a> and <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/agent/knowledge/config.ts" target="_blank" rel="noreferrer">archive configuration</a>.</p><h2 id="understand-a-memory-budget-warning" tabindex="-1">Understand a memory budget warning <a class="header-anchor" href="#understand-a-memory-budget-warning" aria-label="Permalink to &quot;Understand a memory budget warning&quot;">​</a></h2><p>The 8,000-character memory and 3,000-character user budgets are soft composition budgets. <code>overBudget: &quot;error&quot;</code> increases the severity of the generated warning; it does not reject a file write. A zero budget disables that soft check. The loader&#39;s separate hard context limits still apply.</p><p>An over-budget <code>MEMORY.md</code> can appear in context as a compact section index, with short section briefs and instructions to retrieve the full text. Building that index is deterministic and does not rewrite the on-disk memory. If there are no suitable headings, the loader retains full-content handling instead.</p><p>Before consolidating, read the complete source or retrieve its sections. Rewriting the file from only the injected index would lose entries the model cannot currently see. After consolidation, check the source, the next spawned session&#39;s budget banner, and a retrieval of a detail retained in the archive.</p><h2 id="retrieve-personal-or-shared-knowledge" tabindex="-1">Retrieve personal or shared knowledge <a class="header-anchor" href="#retrieve-personal-or-shared-knowledge" aria-label="Permalink to &quot;Retrieve personal or shared knowledge&quot;">​</a></h2><p>Ask for <code>memory_search</code> with <code>corpus: &quot;memory&quot;</code> for this agent&#39;s archive, <code>&quot;shared&quot;</code> for shared knowledge, or <code>&quot;all&quot;</code> for both. Search results contain ranked snippets, source locations, and provenance. Use <code>memory_get</code> for the exact source excerpt before relying on a detail.</p><p>Shared knowledge is enabled by default with project <code>global</code>, root <code>~/.claude-gateway/shared/kb</code>, mode <code>auto</code>, and graph views disabled. A project&#39;s vault is <code>&lt;root&gt;/&lt;project&gt;/</code>, with Markdown under <code>notes/</code> and its own <code>kb.sqlite</code>. Agents assigned the same project share that vault; use deliberate project names when organizing teams or topics.</p><p>Shared promotion and personal memory consolidation have separate mode settings. Set <code>gateway.knowledge.shared.mode: &quot;propose&quot;</code> when you want shared promotion proposals instead of automatic shared writes. Changing dreaming mode alone is not a substitute for checking the shared configuration.</p><p>Source: <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/agent/knowledge/config.ts" target="_blank" rel="noreferrer">shared configuration and paths</a> and <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/agent/knowledge/shared-promote.ts" target="_blank" rel="noreferrer">shared promotion</a>.</p><h2 id="dreaming-schedule-safeguards-and-audit" tabindex="-1">Dreaming schedule, safeguards, and audit <a class="header-anchor" href="#dreaming-schedule-safeguards-and-audit" aria-label="Permalink to &quot;Dreaming schedule, safeguards, and audit&quot;">​</a></h2><p>Dreaming is enabled in <code>auto</code> mode by default. Its built-in start time is 03:00, with up to a 30-minute staggering window across agents, a 30-minute quiet window, a three-day lookback, and at most three proposed changes per run. The review model defaults to <code>claude-haiku-4-5-20251001</code>. An agent active inside the quiet window can have its cycle skipped rather than consolidated immediately.</p><p>Agent dreaming settings override gateway dreaming settings. The timezone falls through agent <code>dreamTimezone</code>, gateway <code>dreamTimezone</code>, <code>gateway.timezone</code>, then UTC. Use <code>mode: &quot;propose&quot;</code> for an audit-only consolidation review, and inspect <code>&lt;workspace&gt;/.dreaming/DREAMS.md</code> before enabling automatic changes.</p><p>Automatic application keeps pre-images under <code>.dreaming/backups/</code>, checks that source content has not changed concurrently, and bounds what proposals can modify. Review applied and skipped counts, not just the presence of a diary entry: a run can propose changes that the safe applier declines. Backups are created for changes that will actually be committed, so a no-op need not produce a new backup.</p><p>Staleness handling defaults to a 90-day TTL, retaining important or sufficiently retrieved entries according to its policy and limiting invalidations to 50 per run. Personal notes under <code>memory/pinned/</code> are searchable but exempt from archive aging. Shared knowledge also has staleness settings; weekly shared reflection is enabled by default for Sunday at 04:00, processing up to five clusters per run.</p><p>Source: <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/agent/dreaming/config.ts" target="_blank" rel="noreferrer">dreaming defaults</a>, <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/agent/dreaming/index.ts" target="_blank" rel="noreferrer">cycle implementation</a>, <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/agent/dreaming/applier.ts" target="_blank" rel="noreferrer">safe applier</a>, and <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/agent/knowledge/lifecycle.ts" target="_blank" rel="noreferrer">archive lifecycle</a>.</p>',32)])])}const m=a(n,[["render",d]]);export{u as __pageData,m as default};
