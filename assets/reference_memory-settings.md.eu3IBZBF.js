import{_ as t,o,c as d,a2 as a}from"./chunks/framework.D1JEzK2q.js";const p=JSON.parse('{"title":"Memory, learning and knowledge settings","description":"","frontmatter":{},"headers":[],"relativePath":"reference/memory-settings.md","filePath":"reference/memory-settings.md"}'),s={name:"reference/memory-settings.md"};function n(r,e,i,c,l,h){return o(),d("div",null,[...e[0]||(e[0]=[a(`<h1 id="memory-learning-and-knowledge-settings" tabindex="-1">Memory, learning and knowledge settings <a class="header-anchor" href="#memory-learning-and-knowledge-settings" aria-label="Permalink to &quot;Memory, learning and knowledge settings&quot;">​</a></h1><p>These settings are read from <code>config.json</code>. Examples are partial objects to merge into an existing configuration. Defaults in the tables refer to runtime fallback values; the installed template may explicitly choose a different value.</p><h2 id="gateway-skilllearning" tabindex="-1"><code>gateway.skillLearning</code> <a class="header-anchor" href="#gateway-skilllearning" aria-label="Permalink to &quot;\`gateway.skillLearning\`&quot;">​</a></h2><p>Controls <a href="./../guide/tools.html">skill learning</a> — agents learning reusable skills from their own work. Telemetry capture is always on; the reviewer/writer/curator honor <code>enabled</code>.</p><table tabindex="0"><thead><tr><th>Field</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>enabled</code></td><td><code>true</code></td><td>Master switch for the reviewer/writer/curator (telemetry is captured regardless)</td></tr><tr><td><code>mode</code></td><td><code>&quot;auto&quot;</code></td><td><code>auto</code> writes skills directly; <code>propose</code> queues them for approval instead</td></tr><tr><td><code>minToolCalls</code></td><td><code>5</code></td><td>Minimum tool calls in a turn before it&#39;s eligible for review</td></tr><tr><td><code>reviewModel</code></td><td><code>claude-haiku-4-5-…</code></td><td>Model used for the background review pass</td></tr><tr><td><code>maxAutoSkills</code></td><td><code>50</code></td><td>Cap on the number of non-pinned <code>origin: auto</code> skills kept per agent (pinned skills are never evicted and don&#39;t count toward the cap)</td></tr><tr><td><code>maxAgeDays</code></td><td><code>30</code></td><td>Curator prunes auto-skills older than this (with too few uses)</td></tr><tr><td><code>minUsesToKeep</code></td><td><code>2</code></td><td>Auto-skills used fewer times than this are prune candidates</td></tr><tr><td><code>maxReviewsPerDay</code></td><td><code>20</code></td><td>Per-day cap on background review runs</td></tr><tr><td><code>pruneHour</code> / <code>pruneTimezone</code></td><td><code>3</code> / <code>UTC</code></td><td>When the daily curator runs; <code>pruneTimezone</code> falls back to <code>gateway.timezone</code> when unset or invalid</td></tr><tr><td><code>notify</code></td><td><code>true</code></td><td>Notify only the originating session (see <a href="./../guide/tools.html">skill notifications</a>); the <code>SKILLS_LEARNED.md</code> diary is written regardless</td></tr></tbody></table><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;gateway&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;skillLearning&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;enabled&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;mode&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;auto&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;notify&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>Per-agent overrides are supported under the agent&#39;s own <code>skillLearning</code> block; unset fields fall back to the gateway default.</p><h2 id="gateway-memory" tabindex="-1"><code>gateway.memory</code> <a class="header-anchor" href="#gateway-memory" aria-label="Permalink to &quot;\`gateway.memory\`&quot;">​</a></h2><p>Memory budget discipline. Self-authored memory files (<code>MEMORY.md</code>, <code>USER.md</code>) that exceed a <strong>soft</strong> char budget get a loud over-budget banner prepended to their <code>CLAUDE.md</code> section at compose time — instead of a silent <code>[TRUNCATED]</code> — nudging the agent to consolidate. The banner reaches the agent on its next spawn (frozen-at-spawn, no restart) and self-heals once the file is back under budget. The banner lives only in the composed <code>CLAUDE.md</code>; the source file on disk is never rewritten with it.</p><table tabindex="0"><thead><tr><th>Field</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>memoryBudgetChars</code></td><td><code>8000</code></td><td>Soft budget for <code>MEMORY.md</code> (<code>0</code> = disabled)</td></tr><tr><td><code>userBudgetChars</code></td><td><code>3000</code></td><td>Soft budget for <code>USER.md</code> (<code>0</code> = disabled)</td></tr><tr><td><code>overBudget</code></td><td><code>&quot;warn&quot;</code></td><td>Banner severity: <code>warn</code> (⚠️) or <code>error</code> (🛑, stronger wording); an unknown value falls back to <code>warn</code></td></tr><tr><td><code>writeRouting</code></td><td><code>false</code> (template: <code>true</code>)</td><td>Inject the <strong>two-tier write contract</strong> into the Memory Rule (<code>MEMORY.md</code> = durable facts; task-log → <code>memory/&lt;topic&gt;.md</code>) and let nightly dreaming route episodic ops out. <code>false</code> = kill-switch (exact pre-routing behavior)</td></tr><tr><td><code>episodicArchiveDir</code></td><td><code>&quot;memory&quot;</code></td><td>Workspace-relative dir episodic notes are written under (validated, path-traversal-guarded)</td></tr></tbody></table><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;gateway&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">    &quot;memory&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: {</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;memoryBudgetChars&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">8000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;userBudgetChars&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;overBudget&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;warn&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;writeRouting&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">true</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">      &quot;episodicArchiveDir&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;memory&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  }</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>The soft budget sits well under the hard per-file limit (still applied as a context safety net); the banner is the primary over-budget signal for memory files.</p><p><strong>Write routing (planning-65).</strong> <code>MEMORY.md</code> is injected into every prompt, so it should hold only <strong>durable semantic facts</strong> (preferences, standing rules, identity, lessons). <strong>Episodic task-log</strong> (completed work, PR/issue status, dated events) belongs in <code>memory/&lt;topic&gt;.md</code> — indexed and retrieved on demand via <code>memory_search</code>, never carried in-prompt. When <code>writeRouting</code> is on, the Memory Rule states this tier contract to the agent, and the nightly dreaming reviewer may emit <code>tier:&quot;episodic&quot;</code> ops that the applier appends to <code>memory/&lt;topic&gt;.md</code> (slug-validated + realpath-confined; a memory-only change ⇒ no session restart). To drain an existing over-budget <code>MEMORY.md</code>, run the one-shot migration <code>node dist/agent/dreaming/migrate-cli.js &lt;workspaceDir&gt; [--apply]</code> — a deterministic terminal sweep (compactor) plus a gated episodic route-out (<code>propose</code> writes <code>.dreaming/migration-plan.md</code>; <code>--apply</code> performs the moves). Pinned sections (<code>## User</code>, <code>## Feedback</code>, <code>## Preferences</code>) are never moved, and every relocated entry stays searchable via <code>memory_search</code> (recall preserved). <strong>planning-67:</strong> with <code>gateway.dreaming.autoRouteOut</code> on (the default), the nightly dream performs this same route-out <strong>automatically</strong> whenever <code>MEMORY.md</code> is over budget — no manual per-agent run — and every over-budget net-shrink <code>remove</code> now <strong>relocates</strong> its block to <code>memory/archive/pruned.md</code> (searchable) before cutting it, so no dream op ever silently forgets.</p><h2 id="gateway-dreaming" tabindex="-1"><code>gateway.dreaming</code> <a class="header-anchor" href="#gateway-dreaming" aria-label="Permalink to &quot;\`gateway.dreaming\`&quot;">​</a></h2><p>Nightly memory <strong>dreaming</strong> — background consolidation of an agent&#39;s long-term memory. A print-only <code>claude -p</code> reviewer (no tools, no <code>--dangerously-skip-permissions</code>) reads a lookback window of the agent&#39;s own session transcripts and proposes memory-consolidation ops. In <strong><code>auto</code></strong> mode (the default) a safe applier writes the ops to <code>MEMORY.md</code>/<code>USER.md</code> (rollback pre-image first; ordered apply with anchor re-resolution; bounded-loss + append-only fallback; net-negative when over budget) — a memory-only change, so no session is restarted. In <strong><code>propose</code></strong> mode the proposals are written <strong>only</strong> to a <code>DREAMS.md</code> diary + JSONL audit under <code>&lt;workspace&gt;/.dreaming/</code> — no memory file is modified (set <code>mode: &quot;propose&quot;</code> to keep this dry-run behavior).</p><table tabindex="0"><thead><tr><th>Field</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>enabled</code></td><td><code>true</code></td><td>Master switch (<code>false</code> ⇒ no scheduler, no run)</td></tr><tr><td><code>mode</code></td><td><code>&quot;auto&quot;</code></td><td><code>auto</code> = apply ops via the safe applier (backup, bounded-loss, net-negative); <code>propose</code> = diary-only dry-run</td></tr><tr><td><code>dreamHour</code> / <code>dreamTimezone</code></td><td><code>3</code> / <code>UTC</code></td><td>When the nightly dream runs (invalid tz → <code>gateway.timezone</code>, then UTC); <code>dreamTimezone</code> falls back to <code>gateway.timezone</code> when unset or invalid</td></tr><tr><td><code>dreamMinute</code></td><td><code>0</code></td><td>Minute-of-hour the dream fires at, paired with <code>dreamHour</code> (0–59). Set with <code>staggerWindowMinutes: 0</code> to fire at an exact <code>HH:MM</code> (e.g. for a controlled re-test)</td></tr><tr><td><code>quietMinutes</code></td><td><code>30</code></td><td>Skip a run if a session was active within this window</td></tr><tr><td><code>lookbackDays</code></td><td><code>3</code></td><td>How far back to scan sessions</td></tr><tr><td><code>maxChangesPerRun</code></td><td><code>3</code></td><td>Cap on proposed ops per run (<code>0</code> ⇒ no-op)</td></tr><tr><td><code>reviewModel</code></td><td><code>claude-haiku-4-5-…</code></td><td>Cheap model for the reviewer</td></tr><tr><td><code>promotionThreshold</code> / <code>minRecallCount</code></td><td><code>0.6</code> / <code>2</code></td><td>Scoring thresholds for promoting a fact</td></tr><tr><td><code>autoRouteOut</code></td><td><code>true</code></td><td>planning-67: in <code>auto</code> mode, drain an <strong>over-budget</strong> <code>MEMORY.md</code> by routing its episodic task-log to <code>memory/&lt;topic&gt;.md</code> automatically each night (archive-safe, pinned excluded, idempotent) instead of a manual per-agent <code>migrate-cli</code>. <code>false</code> = kill-switch</td></tr><tr><td><code>staggerWindowMinutes</code></td><td><code>30</code></td><td>planning-68: spread agents&#39; nightly runs across a window (a deterministic per-agent jitter is added to the delay) so they don&#39;t all fire at <code>dreamHour:00</code> together. Clamped <code>[0,55]</code>; <code>0</code> = disabled (all fire at <code>dreamHour:00</code>)</td></tr><tr><td><code>staleness</code></td><td><em>(object)</em></td><td>Archive staleness GC sub-config (planning-66) — see below</td></tr></tbody></table><p>Per-agent overrides are supported under the agent&#39;s own <code>dreaming</code> block; unset fields fall back to the gateway default. <code>enabled:false</code> or <code>maxChangesPerRun:0</code> makes a run a no-op.</p><blockquote><p><strong>⚠️ Upgrade note:</strong> the default <code>mode</code> for both <code>gateway.dreaming</code> and <code>gateway.knowledge.shared</code> changed from <code>propose</code> (dry-run) to <code>auto</code> (configVersion 1.0.24). Once the K4 applier landed (backup + net-negative + bounded-loss + CAS + never-empty; memory-only write ⇒ no session restart), <code>auto</code> became the intended default: nightly dreaming now applies consolidation to <code>MEMORY.md</code>/<code>USER.md</code> and promotes durable memories to the shared vault. Like the <code>gateway.bind</code> migration, the migrator upgrades the <em>retired</em> <code>propose</code> default to <code>auto</code> once and logs a one-time warning; an explicit <code>mode</code> you set at 1.0.24+ is never touched. To keep dry-run, set <code>mode: &quot;propose&quot;</code> explicitly.</p></blockquote><p><strong>Keeping <code>MEMORY.md</code> near budget (<code>auto</code> mode).</strong> Two mechanisms stop the on-disk <code>MEMORY.md</code> from growing unbounded while preserving recall:</p><ul><li><strong>Deterministic compaction</strong> — before the LLM reviewer, every <code>auto</code> run moves completed/terminal log entries out of <code>MEMORY.md</code> into <code>memory/archive/completed.md</code>, leaving a one-line pointer. It is <strong>domain-agnostic</strong> (not just dev): an entry is archived when its lead line carries an explicit done marker — an UPPERCASE status word (<code>DONE</code>, <code>COMPLETED</code>, <code>RESOLVED</code>, <code>CLOSED</code>, <code>CANCELLED</code>, <code>ARCHIVED</code>, <code>MERGED</code>, <code>SUPERSEDED</code>, <code>OBSOLETE</code>, <code>DEPRECATED</code>, <code>EXPIRED</code>, <code>SHIPPED</code>, <code>FINISHED</code>), a checked task box <code>[x]</code>, a ✅, or a <s>strikethrough</s> — and it works on both list bullets and <code>###</code> entry headers. The archive lives under <code>memory/</code> so it is still indexed and <strong>searchable via <code>memory_search</code></strong> — the agent recalls completed work on demand instead of carrying its full changelog in-prompt. It is conservative (uppercase words only, so prose like &quot;Closes #123&quot;, &quot;we&#39;re not done&quot;, or an unchecked <code>[ ]</code> box is never archived), idempotent, and only moves entries whose lead line matches a terminal marker, preserving their original text in the archive.</li><li><strong>Budget-scaled pruning</strong> — when <code>MEMORY.md</code> is over its soft budget, the reviewer is put in an explicit net-shrink mode (propose only length-reducing ops) and <code>maxChangesPerRun</code> scales up <strong>for removals</strong> (the add cap stays tight), so an over-budget file converges toward budget instead of trickling at a few edits per night.</li><li><strong>Archive staleness GC (<code>gateway.dreaming.staleness</code>, planning-66)</strong> — a deterministic pass that runs next to the compactor (auto mode) to keep the Lane-2 archive&#39;s <strong>search quality</strong> high. This is a <strong>search-quality fix, not a prompt-budget one</strong>: planning-65 already moved task-log off the injected prompt, so the point here is that <code>memory_search</code> should keep surfacing <em>current</em> truth instead of stale/superseded facts. Each nightly run <strong>soft-invalidates</strong> archive entries — superseded ones (a deterministic <code>supersedes/replaces/obsoletes #N</code> match, which finally populates the previously-inert <code>supersedes_key</code>) and aged-out ones (idle-since-last-<strong>retrieval</strong> past <code>staleTtlDays</code> and retrieved fewer than <code>minRetrievalKeep</code> times) — by <strong>moving</strong> them to <code>memory/archive/stale.md</code> and stamping <code>invalid_at</code>. It <strong>never deletes</strong>: a staled entry stays under <code>memory/</code> so it is still indexed and <strong>searchable</strong> . An entry that is <strong>retrieved after</strong> it was invalidated is <strong>promoted back</strong> to the active archive (the recall feedback loop — proof we aged it out too soon). Recall is fed by an append-only read-path log (<code>kb_retrieval_log</code>, gated by <code>recordRetrievals</code>) that the GC folds into each entry&#39;s <code>last_retrieved</code>. High-importance entries (<code>keepImportance</code>) and <strong>pinned</strong> files (<code>memory/pinned/**</code>) are never aged out; evergreen Lane-1 (<code>MEMORY.md</code>/<code>USER.md</code>) is structurally excluded. Every move is CAS-guarded with a timestamped backup, and — being a memory-only write — drops <strong>no live session</strong>. One run may soft-invalidate at most <code>staleness.maxInvalidationsPerRun</code> entries (default <code>50</code>), oldest-idle first, with the remainder resuming on later runs — aging is wall-clock driven, so without a ceiling the first run after anything that widens the GC&#39;s visibility (such as backfilling lifecycle rows for previously invisible sources) would relocate every already-expired entry in one night. Restores are never capped. Kill-switches: <code>staleness.enabled:false</code> (GC no-ops), <code>maxInvalidationsPerRun:0</code> (never invalidates, still restores) and <code>recordRetrievals:false</code> (age falls back to first-seen only).</li></ul><h2 id="gateway-knowledge" tabindex="-1"><code>gateway.knowledge</code> <a class="header-anchor" href="#gateway-knowledge" aria-label="Permalink to &quot;\`gateway.knowledge\`&quot;">​</a></h2><p><strong>Two-lane memory</strong> — a per-agent searchable knowledge archive so an agent can recall what does not fit in the always-injected core. A SQLite/FTS5 index (<code>agents/&lt;id&gt;/kb.sqlite</code>, built on Node&#39;s built-in <code>node:sqlite</code> — no new dependency) covers the agent&#39;s <code>memory/*.md</code> notes plus the evergreen <code>MEMORY.md</code>/<code>USER.md</code>. Every chunk is tagged with <strong>fail-closed provenance</strong> (<code>owner</code>/<code>agent</code>/<code>untrusted</code>/<code>system</code>; unclassified ⇒ <code>untrusted</code>). The index is refreshed by a detached subprocess at session spawn, entirely <strong>off the gateway event loop</strong>.</p><p>Two read-only MCP tools expose it to the agent: <strong><code>memory_search</code></strong> (keyword/FTS5 → ranked snippets with file+line, provenance, importance) and <strong><code>memory_get</code></strong> (bounded, path-traversal-guarded excerpt of a memory-scoped file). When <code>MEMORY.md</code> grows past its <code>gateway.memory</code> soft budget, compose injects a compact <strong>auto-generated section index</strong> + a pointer to <code>memory_search</code> instead of the truncated full text (<strong>core-shrink</strong>) — the on-disk file is never modified and its full content stays searchable. Whenever the archive is on, a short <code>--- MEMORY RETRIEVAL ---</code> note is also injected into every agent&#39;s system prompt so the tools stay discoverable at all times (not only when the file is over budget).</p><table tabindex="0"><thead><tr><th>Field</th><th>Default</th><th>Description</th></tr></thead><tbody><tr><td><code>archive.enabled</code></td><td><code>true</code></td><td>Master switch (<code>false</code> ⇒ complete no-op, no DB created, no core-shrink)</td></tr><tr><td><code>archive.tokenizer</code></td><td><code>&quot;unicode61&quot;</code></td><td>FTS5 tokenizer (<code>&quot;trigram&quot;</code> for CJK/Thai)</td></tr><tr><td><code>archive.chunkTokens</code></td><td><code>400</code></td><td>Target chunk size in ~tokens</td></tr><tr><td><code>archive.chunkOverlap</code></td><td><code>80</code></td><td>Overlap between chunks (clamped below <code>chunkTokens</code>)</td></tr><tr><td><code>shared.enabled</code></td><td><code>true</code></td><td>Enable the cross-agent shared KB</td></tr><tr><td><code>shared.project</code></td><td><code>&quot;global&quot;</code></td><td>Sharing partition key (one safe path segment) — agents with the same value share one vault; <code>&quot;global&quot;</code> ⇒ shared-by-default</td></tr><tr><td><code>shared.root</code></td><td><code>~/.claude-gateway/shared/kb</code></td><td>Shared vault root dir (<code>&lt;root&gt;/&lt;project&gt;/</code>)</td></tr><tr><td><code>shared.mode</code></td><td><code>&quot;auto&quot;</code></td><td>Per-agent→shared promotion mode; <code>auto</code> = promote durable dreamed facts, <code>propose</code> = dry-run</td></tr><tr><td><code>shared.graph</code></td><td><code>false</code></td><td>Compile the memory-wiki graph + dashboards over the shared vault to <code>&lt;vault&gt;/reports/*.md</code> (opt-in). Independent of the dashboard <strong>Knowledge base</strong> tab, which computes its graph on-demand</td></tr><tr><td><code>shared.staleness</code></td><td><em>(object)</em></td><td>Shared-note TTL lifecycle GC; uses the same fields/defaults as <code>dreaming.staleness</code> (whole notes only; no numeric <code>supersedes #N</code> syntax)</td></tr><tr><td><code>reflection.enabled</code></td><td><code>true</code></td><td>Enable the singleton, per-shared-vault reflection scheduler (daily timer; see cadence note below)</td></tr><tr><td><code>reflection.dayOfWeek</code> / <code>hour</code> / <code>minute</code> / <code>timezone</code></td><td><code>0</code> / <code>4</code> / <code>0</code> / <code>UTC</code></td><td><code>hour</code>/<code>minute</code> is the <strong>daily</strong> staleness-GC slot; <code>dayOfWeek</code> selects the weekday that additionally runs LLM consolidation (Sunday 04:00 UTC by default; invalid timezone falls back to <code>gateway.timezone</code>, then <code>UTC</code>)</td></tr><tr><td><code>reflection.maxClustersPerRun</code> / <code>reviewModel</code></td><td><code>5</code> / <code>claude-haiku-4-5-…</code></td><td>Hard cap on changed linked-note clusters per consolidation run and the bounded synthesis model</td></tr></tbody></table><p><strong>Shared KB.</strong> A shared SQLite/FTS5 vault outside any single agent&#39;s workspace lets agents build a common knowledge base. Notes under <code>&lt;root&gt;/&lt;project&gt;/notes/*.md</code> are indexed and reachable via <code>memory_search</code> with <code>corpus:&quot;shared&quot;</code> (the shared vault) or <code>corpus:&quot;all&quot;</code> (this agent&#39;s memory + shared, merged by relevance). Concurrent writers are safe without a lock — atomic note writes (temp+rename) plus a cross-process <code>PRAGMA busy_timeout</code> on the index. Per-agent overrides under the agent&#39;s own <code>knowledge</code> block. The MCP layer runs under Bun, so the read tools query <code>kb.sqlite</code> via <code>bun:sqlite</code>. Two write paths feed the vault, sharing one freeform-name namespace (issue #386, no agent-id prefix, no ownership scoping): the nightly dreaming promoter (gated by <code>mode:&quot;auto&quot;</code>; it promotes only content that carries a real fact — content that is nothing but <code>MEMORY.md</code> index-pointer bullets is skipped, since those links resolve only inside the promoting agent&#39;s own workspace — and names each note after the proposal&#39;s <code>topic</code> slug when the reviewer supplied one, falling back to its <code>reason</code>, so a recurring fact updates the same note across nights instead of piling up near-duplicates; a fallback name that reads as an editing instruction rather than the name of a fact is passed over, and the note is named from the fact itself instead — the promotion is only abandoned when nothing nameable remains, and every skip is logged — including a write the note-size cap refuses and an unexpected write failure. A name that doesn&#39;t collide is checked against a near-duplicate search, but an unattended <strong>merge</strong> now also requires real token containment against the candidate — below that bar the fact gets its own note, since two notes are recoverable while two unrelated facts fused into one are not. <code>[[wikilink]]</code>s to related notes use a lower bar than merges, because a link is additive where a merge is destructive — and they are attached whether the fact merges or lands as a new note, so a note below the merge bar is never a disconnected graph node. Containment is scored against each candidate&#39;s full body rather than the matched chunk, though against a capped seed — the bar means &quot;half of the fact&#39;s leading topic words are already here&quot;, not half of the whole fact. Retired <code>stale__*</code> notes are never merge targets; a recurrence of a retired name folds the retired body back in and removes the twin on both the create and the update path, because a retired note stays searchable and a twin beside a live note of the same name would answer every query twice forever. The twin is only dropped once the merged write lands (issue #398)) and the <strong><code>memory_shared_create</code></strong>/<strong><code>memory_shared_get</code></strong>/<strong><code>memory_shared_update</code></strong>/<strong><code>memory_shared_delete</code></strong> MCP tools, which let any agent create, read, update, or delete any note on demand regardless of <code>mode</code>. <code>memory_shared_create</code> warns instead of writing when it finds content-similar existing notes (pass <code>confirm:true</code> to proceed — related notes get <code>[[wikilink]]</code>ed into the new note rather than left disconnected); <code>memory_shared_update</code> warns instead of writing when the edit would drop 50%+ of the existing note&#39;s lines (same <code>confirm:true</code> escape hatch). Immediate reindex after every write or delete.</p><p><strong>Shared lifecycle + reflection (issues #392, #398).</strong> Each shared note receives a stable whole-file lifecycle identity during indexing — including notes whose content has not changed since they were first indexed, which are backfilled from their source mtime so their real age is preserved. Its deterministic TTL GC runs <strong>daily</strong>, soft-invalidating aged low-recall notes by moving them to <code>notes/stale__&lt;name&gt;.md</code> (never deleting them from the searchable vault); a retrieval after invalidation restores the original active name. Shared <code>memory_search</code> and <code>memory_shared_get</code> reads feed the same append-only retrieval log as personal archive recall. The singleton reflection scheduler runs <strong>once per resolved shared-vault root</strong>, not once per agent, and fires <strong>daily</strong> at <code>hour:minute</code> (a fire that lands a hair early re-arms on the <em>next</em> day&#39;s slot rather than serving the same one twice): every fire runs the inexpensive TTL GC (no model call), while graph/LLM consolidation runs only on <code>dayOfWeek</code> — and even then is skipped when <code>kb_index_state.revision</code> has not changed since the prior consolidation. Weekly model spend is therefore unchanged, while a note that is retired and then retrieved returns to the active set within a day instead of up to a week. For changed vaults it clusters only active wikilink-connected notes deterministically, then makes at most <code>reflection.maxClustersPerRun</code> bounded reviewer calls to merge genuinely duplicate clusters; related-but-distinct notes remain merely linked.</p><p><strong>Knowledge base viewer.</strong> The web dashboard&#39;s <strong>Knowledge base</strong> tab renders the shared vault as an Obsidian-style force-directed graph (nodes = notes sized by link degree and coloured by <code>type</code>; edges = <code>[[wiki-links]]</code>; contradicting claims and stale notes are flagged). It is fed by <code>GET /knowledge/graph</code>, which computes the model <strong>on-demand</strong> from the vault (no dependency on <code>shared.graph</code> or the nightly reindex). When the vault is empty it shows a clearly-labelled demo dataset (with a size selector for scale testing). A <strong>source</strong> selector switches the graph between the cross-agent Shared KB and any single agent&#39;s own Lane-2 memory (<code>workspace/memory</code>), a node <strong>search</strong> box filters the graph, and clicking a node opens its full note (fetched via <code>GET /knowledge/note</code>) rendered as Markdown below the graph.</p><p><strong>Nightly dreaming viewer.</strong> A <strong>Nightly dreaming</strong> tab renders each agent&#39;s memory-consolidation audit trail (<code>.dreaming/DREAMS.md</code> + <code>promotions.jsonl</code>) as a newest-first timeline of runs — mode (propose/auto), outcome, the proposed/applied changes with scores + anchors, and per-run token/session counts — fed by <code>GET /knowledge/dreams</code> and filterable by agent. For a <code>propose</code>-mode run you can <strong>accept</strong> proposals directly from the tab: an <strong>Accept</strong> button per proposal (and <strong>Accept all</strong> per run) POSTs to <code>POST /knowledge/dreams/apply</code>, which applies the selected ops to <code>MEMORY.md</code>/<code>USER.md</code> through the same K4 safe applier auto mode uses (backup + bounded-loss + net-negative + CAS; memory-only ⇒ no restart) and — when the shared KB is <code>auto</code> — promotes applied <code>add</code>s to the shared vault. Accepts are idempotent (recorded to <code>.dreaming/accepted.jsonl</code>); applied proposals show ✓ and a proposal whose anchor has since drifted is safely skipped and stays pending for a later retry.</p><p>Implementation: <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/config/loader.ts" target="_blank" rel="noreferrer">config loader</a>, <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/config.template.json" target="_blank" rel="noreferrer">configuration template</a>.</p>`,29)])])}const u=t(s,[["render",n]]);export{p as __pageData,u as default};
