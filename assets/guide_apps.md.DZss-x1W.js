import{_ as a,o as t,c as s,a2 as i}from"./chunks/framework.D1JEzK2q.js";const u=JSON.parse('{"title":"Apps","description":"","frontmatter":{},"headers":[],"relativePath":"guide/apps.md","filePath":"guide/apps.md"}'),n={name:"guide/apps.md"};function o(r,e,p,d,l,c){return t(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="apps" tabindex="-1">Apps <a class="header-anchor" href="#apps" aria-label="Permalink to &quot;Apps&quot;">​</a></h1><p>The App Store installs Docker Compose applications and exposes their declared ports through the gateway proxy. Some apps also include an agent or host-script bridge.</p><h2 id="inspect-and-install" tabindex="-1">Inspect and install <a class="header-anchor" href="#inspect-and-install" aria-label="Permalink to &quot;Inspect and install&quot;">​</a></h2><p>You need a working Docker/Compose environment and an admin API key. Start by listing installed apps:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">claude-gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span></span></code></pre></div><p>Install from a registry name, a GitHub URL, or an existing local app directory. For example, for a local app you have reviewed:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">claude-gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> install</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> ./my-app</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --wait</span></span></code></pre></div><p>The source directory must contain the gateway app manifest and its required files. <code>--wait</code> polls the installation job and streams logs. Without it, the command returns an accepted <code>jobId</code>; inspect completion with <code>claude-gateway api GET /v1/apps/jobs/JOB_ID</code>.</p><p>App URLs follow <code>/app/:name/:portName/*</code>. Verify the job&#39;s final result, then load the app and exercise a small action before calling the installation successful.</p><h2 id="operate-an-app" tabindex="-1">Operate an app <a class="header-anchor" href="#operate-an-app" aria-label="Permalink to &quot;Operate an app&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">claude-gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> restart</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> APP_NAME</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">claude-gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> app</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span></span></code></pre></div><p>Use <code>--env-file</code> when an install needs secrets. An uninstall removes containers and installed files while retaining backups; it asks for confirmation. Review <code>gateway.appBackup</code>, <code>gateway.appRestore</code>, and <code>gateway.appHousekeeping</code> when planning storage and recovery.</p><p>See the <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/apps/compose-generator.ts" target="_blank" rel="noreferrer">manifest generator</a> and <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/apps/installer.ts" target="_blank" rel="noreferrer">installation implementation</a> for validation and lifecycle details. See <a href="./orchestration.html">orchestration</a> for container admission and delegated task lifecycle.</p><h2 id="manifest-and-installation-lifecycle" tabindex="-1">Manifest and installation lifecycle <a class="header-anchor" href="#manifest-and-installation-lifecycle" aria-label="Permalink to &quot;Manifest and installation lifecycle&quot;">​</a></h2><p>An app source contains <code>app.yaml</code>. The gateway validates the manifest, derives its canonical name, generates <code>docker-compose.yml</code> and <code>.env</code>, then builds/starts the declared services and records the installed app. Declared port names identify proxy destinations, while host-port overrides resolve local conflicts. Duplicate or disallowed host ports are rejected during generation.</p><p>A returned installation <code>jobId</code> means the operation was accepted. Follow its logs until the job reaches a terminal result. A container can start while the application behind its port still has configuration or database errors, so test a real page or API call after the job completes. When installing from a local directory, provide a path visible to the gateway host.</p><p>During gateway startup, apps previously marked running may be restored asynchronously. App responses expose <code>restoring</code> during recovery and <code>restoreError</code>/<code>restoreFailedAt</code> when that boot attempt failed. This is especially relevant after moving app data to a host without cached images: rebuilding may be needed before the app becomes reachable. <code>gateway.appRestore</code> sets the build/start budgets.</p><p>Source: <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/apps/installer.ts" target="_blank" rel="noreferrer">installer</a> and <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/api/apps-router.ts" target="_blank" rel="noreferrer">app API restore status</a>.</p><h2 id="app-agents-and-runtime-boundaries" tabindex="-1">App agents and runtime boundaries <a class="header-anchor" href="#app-agents-and-runtime-boundaries" aria-label="Permalink to &quot;App agents and runtime boundaries&quot;">​</a></h2><p>An app can declare an agent workspace. The gateway registers it as an agent and links its workspace to the app&#39;s installed directory. Its generated agent container mounts that workspace at <code>/workspace</code>, uses the host user&#39;s numeric identity, drops all Linux capabilities, and enables <code>no-new-privileges</code>. Agent media is mounted at its host-identical absolute path so uploaded images and screenshots resolve correctly.</p><p>The container receives read-only host binary mounts and a read-only staged Claude configuration seed. The seed is copied into a writable container configuration file at container start, allowing Claude to perform atomic rewrites. Refreshing the staged seed on gateway reconciliation does not itself restart an already running container; check the container lifecycle when an account change is not reflected.</p><p>These are explicit mounts and execution boundaries, not a guarantee that arbitrary app code is harmless. Review the generated Compose file, app services, port exposure, and any declared host-script bridge. The bridge exists to run approved app scripts through the host socket, so its declared capabilities matter when assessing what the app can access.</p><p>Source: <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/apps/agent-manager.ts" target="_blank" rel="noreferrer">app agent manager</a>, <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/apps/compose-generator.ts" target="_blank" rel="noreferrer">Compose generation</a>, and <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/apps/socket-server.ts" target="_blank" rel="noreferrer">host socket server</a>.</p><h2 id="back-up-and-restore-application-data" tabindex="-1">Back up and restore application data <a class="header-anchor" href="#back-up-and-restore-application-data" aria-label="Permalink to &quot;Back up and restore application data&quot;">​</a></h2><p>The app API provides asynchronous backup and restore operations:</p><table tabindex="0"><thead><tr><th>Request</th><th>Result</th></tr></thead><tbody><tr><td><code>POST /v1/apps/APP_NAME/backup</code></td><td>Starts a backup job</td></tr><tr><td><code>GET /v1/apps/APP_NAME/backups</code></td><td>Lists available backups, newest first</td></tr><tr><td><code>POST /v1/apps/APP_NAME/restore</code> with <code>{&quot;backupId&quot;:&quot;BACKUP_ID&quot;}</code></td><td>Starts a restore job</td></tr><tr><td><code>DELETE /v1/apps/APP_NAME/backups/BACKUP_ID</code></td><td>Deletes one backup</td></tr><tr><td><code>GET /v1/apps/jobs/JOB_ID</code></td><td>Shows operation progress and result</td></tr></tbody></table><p>For example, inspect existing backups with:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">claude-gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> api</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> GET</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> /v1/apps/APP_NAME/backups</span></span></code></pre></div><p>A backup stops the app for a snapshot, archives named volumes and eligible app-local bind-mount directories, and captures <code>.env</code>, <code>app.yaml</code>, and Compose configuration. It attempts to restart the app in a <code>finally</code> path even if archiving fails. Check both the job and the app state afterward. Volume archives are created with helper containers to preserve file ownership without changing ownership of host data.</p><p>Backups normally live in <code>&lt;appsDir&gt;/.backups/&lt;app&gt;/</code>. The default policy keeps the latest three backups and prunes archives older than 30 days. <code>gateway.appBackup</code> controls retention and daily cleanup. Treat backup archives as sensitive because they include <code>.env</code> and application data, and copy required recovery points outside the deployment host.</p><p>Restoring replaces the relevant volume content and configuration before starting the app. Plan for downtime and verify a known record after restore. Uninstall attempts a best-effort backup, but a failed backup does not block the requested uninstall; create and verify an explicit backup first when recovery is required. Backups are retained after uninstall.</p><p>App housekeeping reports orphaned Docker volumes instead of automatically deleting them. Check ownership and recovery needs before removing such volumes yourself.</p><p>Source: <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/apps/installer.ts" target="_blank" rel="noreferrer">backup, restore, and housekeeping implementation</a>.</p><h2 id="orchestration-container-admission" tabindex="-1">Orchestration container admission <a class="header-anchor" href="#orchestration-container-admission" aria-label="Permalink to &quot;Orchestration container admission&quot;">​</a></h2><p>Before task execution, the gateway inspects the app-agent container. It must be running, unprivileged, drop all capabilities, and enable <code>no-new-privileges</code>. Host/container-shared networking, PID namespace overrides, extra capabilities, and device mounts are rejected. Mounts are checked against the agent workspace, media, and recognized read-only runtime/seed mounts; Docker/containerd sockets and unrelated host mounts are denied.</p><p>Execution and skill discovery happen through <code>docker exec</code>. If validation fails, fix the app&#39;s declared runtime and container state; a host retry is forbidden. A missing <code>/workspace</code> binding produces <code>CONTAINER_WORKSPACE_REQUIRED</code>; an unsafe runtime produces <code>CONTAINER_ISOLATION_REQUIRED</code> or <code>CONTAINER_HOST_MOUNT_DENIED</code>.</p><p>The container receives a small task-scoped MCP bridge rather than the host module loader. Artifacts are read inside the container and imported into a gateway-owned spool, with allowed paths under <code>/workspace/</code> or <code>/tmp/</code>, regular-file checks, and a 50 MiB size limit. This avoids resolving an app-controlled path in the host filesystem. Creating a file alone does not deliver it: the task must stage it and complete successfully.</p><p>Source: <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/orchestration/container.ts" target="_blank" rel="noreferrer">container validation, bridge, and artifact import</a>.</p>`,38)])])}const g=a(n,[["render",o]]);export{u as __pageData,g as default};
