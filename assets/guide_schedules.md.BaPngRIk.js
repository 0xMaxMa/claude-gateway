import{_ as t,o as a,c as s,a2 as i}from"./chunks/framework.D1JEzK2q.js";const u=JSON.parse('{"title":"Schedules and heartbeats","description":"","frontmatter":{},"headers":[],"relativePath":"guide/schedules.md","filePath":"guide/schedules.md"}'),n={name:"guide/schedules.md"};function r(o,e,d,h,c,l){return a(),s("div",null,[...e[0]||(e[0]=[i(`<h1 id="schedules-and-heartbeats" tabindex="-1">Schedules and heartbeats <a class="header-anchor" href="#schedules-and-heartbeats" aria-label="Permalink to &quot;Schedules and heartbeats&quot;">​</a></h1><p>Use <code>HEARTBEAT.md</code> for workspace-defined proactive behavior. Use the cron API or CLI when you need managed jobs, explicit run history, and manual triggering.</p><h2 id="add-a-heartbeat" tabindex="-1">Add a heartbeat <a class="header-anchor" href="#add-a-heartbeat" aria-label="Permalink to &quot;Add a heartbeat&quot;">​</a></h2><p>Put this YAML in the agent&#39;s <code>HEARTBEAT.md</code>:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">tasks</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">:</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">morning-brief</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    cron</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;0 8 * * *&quot;</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    prompt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Give a brief morning summary.&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  - </span><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">check-in</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    interval</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">6h</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">    prompt</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;Check if there are any reminders to send.&quot;</span></span></code></pre></div><p>Cron expressions use five fields: minute, hour, day, month, weekday. Supported interval examples include <code>30m</code>, <code>1h</code>, <code>6h</code>, <code>1d</code>, and <code>1w</code>. Heartbeats use the gateway process timezone; managed cron jobs have their own timezone and default to UTC. The default proactive rate limit is 30 minutes; a <code>HEARTBEAT_OK</code> reply suppresses the Telegram message.</p><h2 id="inspect-a-managed-job" tabindex="-1">Inspect a managed job <a class="header-anchor" href="#inspect-a-managed-job" aria-label="Permalink to &quot;Inspect a managed job&quot;">​</a></h2><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">claude-gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> crons</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> list</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> --agent</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> assistant</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">claude-gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> crons</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> get</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> JOB_ID</span></span>
<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">claude-gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> crons</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> runs</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> JOB_ID</span></span></code></pre></div><p>Replace <code>JOB_ID</code> with an ID from the list. Check the schedule, enabled state, delivery configuration, and latest run result. To intentionally execute it immediately:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">claude-gateway</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> crons</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> run</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> JOB_ID</span></span></code></pre></div><p>This runs the job&#39;s real action. Confirm the completed run and its delivery; an accepted trigger alone does not establish success. Use <code>claude-gateway crons create --help</code> for the current creation flags and the <a href="./../reference/cli-api.html">API reference</a> for request bodies and delivery options.</p><p>Schedules trigger work; the <a href="./orchestration.html">orchestration engine</a> manages delegated execution and task state.</p><h2 id="choose-the-right-scheduler" tabindex="-1">Choose the right scheduler <a class="header-anchor" href="#choose-the-right-scheduler" aria-label="Permalink to &quot;Choose the right scheduler&quot;">​</a></h2><table tabindex="0"><thead><tr><th>Behavior</th><th>Workspace heartbeat</th><th>Managed cron job</th></tr></thead><tbody><tr><td>Definition</td><td><code>HEARTBEAT.md</code> YAML</td><td>Persisted job created through CLI/API/tools</td></tr><tr><td>Action</td><td>Agent prompt</td><td>Shell command or agent prompt</td></tr><tr><td>Schedule</td><td>Five-field cron or supported interval shorthand</td><td>Recurring cron or one-shot <code>at</code></td></tr><tr><td>Timezone</td><td>Gateway process timezone</td><td>Explicit job timezone, otherwise UTC</td></tr><tr><td>Quiet outcome</td><td><code>HEARTBEAT_OK</code> suppresses a proactive message</td><td>Inspect execution and configured delivery</td></tr><tr><td>State</td><td>Heartbeat run history</td><td>Enabled state, counters, last status/error, run logs</td></tr></tbody></table><p>The heartbeat parser requires a nonempty <code>name</code> and <code>prompt</code> and exactly one of <code>cron</code> or <code>interval</code>. Place <code>tasks:</code> at the root of the YAML; the parser stops collecting at an unindented Markdown heading. Invalid YAML can yield no tasks, while invalid task fields produce a parse error in the scheduler log. Always verify the reported loaded-task count after editing.</p><h2 id="intervals-are-wall-clock-schedules" tabindex="-1">Intervals are wall-clock schedules <a class="header-anchor" href="#intervals-are-wall-clock-schedules" aria-label="Permalink to &quot;Intervals are wall-clock schedules&quot;">​</a></h2><p>Heartbeat shorthand converts to cron; it is not elapsed time since the previous completion:</p><table tabindex="0"><thead><tr><th>Shorthand</th><th>Cron equivalent</th><th>Meaning</th></tr></thead><tbody><tr><td><code>30m</code></td><td><code>*/30 * * * *</code></td><td>At minutes 0 and 30</td></tr><tr><td><code>6h</code></td><td><code>0 */6 * * *</code></td><td>At 00:00, 06:00, 12:00, 18:00</td></tr><tr><td><code>1d</code></td><td><code>0 0 * * *</code></td><td>At midnight</td></tr><tr><td><code>1w</code></td><td><code>0 0 * * 0</code></td><td>At midnight on Sunday</td></tr></tbody></table><p>Minute values are 1–59, hour values 1–23, and day/week support only <code>1d</code>/<code>1w</code>. For example, <code>2d</code> and <code>24h</code> are not supported shorthand. The 30-minute proactive rate limit is shared across the agent&#39;s heartbeat tasks, so a recently sent proactive message can suppress another task. Inspect the <code>suppressed</code> and <code>rateLimited</code> result fields when delivery is absent. Heartbeat response collection defaults to a 60-second timeout.</p><p>Source: <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/heartbeat/parser.ts" target="_blank" rel="noreferrer">heartbeat parser</a> and <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/cron/scheduler.ts" target="_blank" rel="noreferrer">heartbeat scheduler</a>.</p><h2 id="managed-execution-persistence-and-restart-behavior" tabindex="-1">Managed execution, persistence, and restart behavior <a class="header-anchor" href="#managed-execution-persistence-and-restart-behavior" aria-label="Permalink to &quot;Managed execution, persistence, and restart behavior&quot;">​</a></h2><p>A managed job defaults to <code>scheduleKind: &quot;cron&quot;</code>, <code>type: &quot;command&quot;</code>, and enabled. An agent task instead needs <code>type: &quot;agent&quot;</code>, an agent ID, and a prompt. One-shot jobs use <code>scheduleKind: &quot;at&quot;</code> with <code>scheduleAt</code>. Prefer an explicit timezone for recurring jobs and an explicit offset in a one-shot timestamp.</p><p>By default jobs persist in <code>~/.claude-gateway/crons.json</code>, with run logs in <code>~/.claude-gateway/cron-runs/</code>; the manager retains the latest 100 log entries per job. Agent tasks use a job-specific <code>cron-&lt;jobId&gt;</code> session. Without orchestration their default timeout is 120 seconds. Shell execution has a fixed 120-second timeout in the current implementation.</p><p>On startup, enabled recurring jobs that previously ran can receive one catch-up execution when a scheduled tick was missed. Catch-up considers the job&#39;s timezone, is capped at five jobs per startup, and staggers them five seconds apart. It does not replay every missed tick. Never-run recurring jobs wait for their normal schedule; past one-shot jobs are handled by their own scheduling path.</p><p>For diagnosis, compare <code>lastRunAt</code>, <code>lastStatus</code>, <code>lastError</code>, <code>consecutiveErrors</code>, and the run log with the intended action. Then verify Telegram or Discord delivery separately. Restarting the gateway can trigger catch-up, so account for that before restarting a scheduler that performs external actions.</p><p>Source: <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/cron/manager.ts" target="_blank" rel="noreferrer">managed cron implementation</a>.</p><h2 id="scheduled-agent-work-with-orchestration" tabindex="-1">Scheduled agent work with orchestration <a class="header-anchor" href="#scheduled-agent-work-with-orchestration" aria-label="Permalink to &quot;Scheduled agent work with orchestration&quot;">​</a></h2><p>With orchestration enabled, a scheduled agent prompt can create managed execution tasks. The cron path waits for those tasks before producing its result and uses stable job-scoped ownership across restarts. It adds a fresh-run instruction so previous scheduled results are treated as history rather than current observations.</p><p>An explicit job <code>timeoutMs</code> takes precedence. Otherwise the orchestration cron wait uses the worker total deadline plus the conversation decision ceiling when a worker total deadline is configured. With the default <code>tasks.maxDurationMs: 0</code>, that outer wait has no total deadline; worker startup, response, and inactivity controls still apply. A timeout log can include <code>phase</code>, elapsed time, and idle time, which helps distinguish slow startup from a task that stopped making progress.</p><p>Source: <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/cron/manager.ts" target="_blank" rel="noreferrer">cron orchestration integration</a>.</p>`,30)])])}const k=t(n,[["render",r]]);export{u as __pageData,k as default};
