import{_ as t,o,c as a,a2 as d}from"./chunks/framework.D1JEzK2q.js";const g=JSON.parse('{"title":"Workspace File API","description":"","frontmatter":{},"headers":[],"relativePath":"api/workspace.md","filePath":"api/workspace.md"}'),s={name:"api/workspace.md"};function n(i,e,r,c,l,h){return o(),a("div",null,[...e[0]||(e[0]=[d(`<h1 id="workspace-file-api" tabindex="-1">Workspace File API <a class="header-anchor" href="#workspace-file-api" aria-label="Permalink to &quot;Workspace File API {#workspace-file-api}&quot;">​</a></h1><p>Read and write an agent&#39;s workspace identity files via the API. The gateway&#39;s file watcher recomposes <code>CLAUDE.md</code> on disk after a write.</p><p><strong>Allowed filenames:</strong> <code>SOUL.md</code>, <code>USER.md</code>, <code>MEMORY.md</code>, <code>AGENTS.md</code>, <code>HEARTBEAT.md</code>, <code>IDENTITY.md</code></p><p><strong>How a write reaches running sessions (frozen-at-spawn).</strong> <code>CLAUDE.md</code> is read by a session only at spawn — a live process never re-reads it, so a change applies on the session&#39;s <strong>next spawn</strong>. The watcher therefore never SIGKILLs a live session just to push a change; it recomposes <code>CLAUDE.md</code> and tiers the restart by change class:</p><table tabindex="0"><thead><tr><th>Changed file(s)</th><th>Effect on running sessions</th></tr></thead><tbody><tr><td><code>MEMORY.md</code>, <code>USER.md</code> (memory)</td><td><strong>No session is restarted.</strong> The change applies on each session&#39;s next natural spawn. A memory write can never drop a live session.</td></tr><tr><td><code>SOUL.md</code>, <code>AGENTS.md</code>, <code>IDENTITY.md</code> (identity)</td><td>Busy sessions are skipped; idle sessions are <strong>deferred</strong> (respawn on their next message) — never SIGKILLed mid-idle.</td></tr><tr><td><code>HEARTBEAT.md</code> / other</td><td>Normal restart-or-defer (idle sessions restart now).</td></tr></tbody></table><p><strong>Memory budget (<code>gateway.memory</code>).</strong> Self-authored memory files (<code>MEMORY.md</code>, <code>USER.md</code>) that exceed a <strong>soft char budget</strong> get a loud, actionable over-budget banner prepended to their <code>CLAUDE.md</code> section at compose time — instead of a silent <code>[TRUNCATED]</code> — so the agent consolidates on its next spawn (frozen-at-spawn, no restart). The soft budget sits well under the hard per-file limit (still applied as a context safety net); the banner is the primary signal for memory files. Config (global, injected by migration at <code>configVersion</code> <code>1.0.19</code>):</p><table tabindex="0"><thead><tr><th>Field</th><th>Default</th><th>Meaning</th></tr></thead><tbody><tr><td><code>memoryBudgetChars</code></td><td><code>8000</code></td><td>Soft budget for <code>MEMORY.md</code> (<code>0</code> = disabled).</td></tr><tr><td><code>userBudgetChars</code></td><td><code>3000</code></td><td>Soft budget for <code>USER.md</code> (<code>0</code> = disabled).</td></tr><tr><td><code>overBudget</code></td><td><code>&quot;warn&quot;</code></td><td>Banner severity: <code>&quot;warn&quot;</code> (⚠️) or <code>&quot;error&quot;</code> (🛑, stronger wording). An unknown value falls back to <code>&quot;warn&quot;</code>.</td></tr><tr><td><code>writeRouting</code></td><td><code>false</code> (template: <code>true</code>)</td><td>planning-65: inject the two-tier write contract (durable → <code>MEMORY.md</code>/<code>USER.md</code>; episodic task-log → <code>memory/&lt;topic&gt;.md</code>) into the Memory Rule and let dreaming route episodic ops out. <code>false</code> = kill-switch (exact pre-routing behavior).</td></tr><tr><td><code>episodicArchiveDir</code></td><td><code>&quot;memory&quot;</code></td><td>Workspace-relative dir episodic notes land under (slug-validated + realpath-confined).</td></tr></tbody></table><p>A memory file under its budget composes cleanly (no banner). Non-memory files are unaffected. A hard-reject memory tool (refuse an over-budget write) is a planned follow-up; v1 is the compose banner only.</p><p><strong>Write routing (<code>gateway.memory.writeRouting</code>, planning-65).</strong> With routing on, the Memory Rule tells the agent that <code>MEMORY.md</code> (injected every prompt) holds only durable facts and that episodic task-log belongs in <code>memory/&lt;topic&gt;.md</code> (searchable via <code>memory_search</code>, not in-prompt). The nightly dreaming reviewer may then emit <code>tier:&quot;episodic&quot;</code> <code>add</code> ops, which the applier appends to <code>memory/&lt;topic&gt;.md</code> (slug <code>^[a-z0-9-]{1,64}$</code>, realpath-confined under the workspace; a memory-only change ⇒ no session restart). A one-shot CLI drains an existing over-budget file: <code>node dist/agent/dreaming/migrate-cli.js &lt;workspaceDir&gt; [--apply]</code> runs the deterministic terminal sweep (compactor) plus a gated episodic route-out (<code>propose</code> writes <code>.dreaming/migration-plan.md</code>; <code>--apply</code> moves). Pinned sections (<code>## User</code>/<code>## Feedback</code>/<code>## Preferences</code>) are never moved; relocated content stays searchable (recall preserved). <strong>planning-67 (<code>gateway.dreaming.autoRouteOut</code>, default <code>true</code>):</strong> the nightly dream runs this route-out <strong>automatically</strong> whenever <code>MEMORY.md</code> is over budget (after the compactor, before the reviewer — transcript-independent, idempotent, pinned-excluded, unbounded by <code>maxChangesPerRun</code>), so no per-agent CLI is needed; and an over-budget net-shrink <code>remove</code> <strong>relocates</strong> its block to <code>memory/archive/pruned.md</code> (searchable) before cutting it — closing the prior hole where a <code>remove</code> deleted without archiving. No dream op deletes recall; all writes are memory-only ⇒ no session restart.</p><p><strong>Nightly dreaming (<code>gateway.dreaming</code>).</strong> A nightly background pass consolidates memory: a print-only <code>claude -p</code> reviewer (no tools, no <code>--dangerously-skip-permissions</code>) reads a lookback window of the agent&#39;s own session transcripts and proposes memory-consolidation ops. In <strong><code>auto</code></strong> mode (the default, planning-64 K4) the ops are applied to <code>MEMORY.md</code>/<code>USER.md</code> through a safe applier: a <strong>rollback pre-image</strong> is written to <code>.dreaming/backups/</code> first (retention-capped) and only when a mutation actually commits; a <code>replace</code>/<code>remove</code> anchor is applied by <strong>index splice</strong> (so <code>$</code> in the new text is literal) and is <strong>skipped when ambiguous</strong> (the target occurs more than once); a <strong>bounded-loss</strong> guard measures <strong>gross original content deleted</strong> (an <code>add</code> cannot mask a large delete) — capped at 25% under budget, raised for deletions when the file is <strong>over budget</strong> (removals are the shrink lever) but never a near-total wipe; over budget, an <code>add</code> (or a <code>replace</code> that would grow the file) is skipped (net-negative); and just before the atomic rename the on-disk content is re-checked against the snapshot (<strong>CAS</strong>) so a concurrent live-agent edit is never clobbered. Writing memory is a memory-only change ⇒ <strong>no session restart</strong> (Part A). In <strong><code>propose</code></strong> mode (set <code>mode: &quot;propose&quot;</code> to opt back into dry-run) proposals are written <strong>only</strong> to <code>DREAMS.md</code> + JSONL audit under <code>&lt;workspace&gt;/.dreaming/</code> — no memory file is mutated. When the shared KB is <code>auto</code>, only the <code>add</code>s the applier <strong>actually wrote</strong> are promoted to the shared vault (never a locally-skipped proposal), and only when the content carries a real fact — an <code>add</code> whose content is nothing but <code>MEMORY.md</code> index-pointer bullets is skipped, because those links resolve only inside the promoting agent&#39;s own workspace (issue #398). A durable proposal may also carry an optional <code>topic</code> kebab-slug naming the fact; it becomes the shared note&#39;s name when present. Runs on a nightly scheduler (<code>dreamHour</code>/<code>dreamMinute</code>/<code>dreamTimezone</code>), skips when a session was active within <code>quietMinutes</code>, and is a no-op when <code>enabled:false</code> or <code>maxChangesPerRun:0</code>. <code>dreamMinute</code> (0–59, default 0) pairs with <code>dreamHour</code> for minute-level scheduling — set it with <code>staggerWindowMinutes: 0</code> to fire at an exact <code>HH:MM</code> (e.g. for a controlled re-test). <strong>planning-68:</strong> to avoid a <code>dreamHour:00</code> thundering-herd across agents (every agent&#39;s timer fires at the same instant, each spawning a route-out + reviewer <code>claude -p</code>), a <strong>deterministic per-agent jitter</strong> (a stable hash of <code>agentId</code> in <code>[0, staggerWindowMinutes*60s)</code>, not <code>Math.random</code>) is added to each agent&#39;s scheduled delay, spreading runs across the window; <code>staggerWindowMinutes</code> defaults to <code>30</code>, is clamped <code>[0,55]</code> (so the offset never crosses the next hour), and <code>0</code> disables it (exact prior behavior). See the README <code>gateway.dreaming</code> reference for all fields.</p><p><strong>Archive staleness GC (<code>gateway.dreaming.staleness</code>).</strong> A deterministic nightly pass (planning-66) that runs next to the compactor in <code>auto</code> mode to keep the Lane-2 archive&#39;s <strong>search quality</strong> high — it is a <strong>search-quality fix, not a prompt-budget one</strong> (planning-65 already moved task-log off the injected prompt). It <strong>soft-invalidates</strong> archive entries and <strong>never deletes</strong> them: a <strong>superseded</strong> entry (a deterministic <code>supersedes</code>/<code>replaces</code>/<code>obsoletes #N</code> match — which finally populates the previously-inert <code>supersedes_key</code>) or an <strong>aged-out</strong> entry (idle-since-last-<strong>retrieval</strong> past <code>staleTtlDays</code> and retrieved fewer than <code>minRetrievalKeep</code> times) is <strong>moved</strong> to <code>memory/archive/stale.md</code> with an <code>invalid_at</code> stamp, staying under <code>memory/</code> so it remains indexed and <code>memory_search</code>-able. An entry <strong>retrieved after</strong> invalidation is <strong>promoted back</strong> to the active archive (recall feedback). Recall is fed by an append-only read-path log (<code>kb_retrieval_log</code>), written fire-and-forget by the Bun read tool through a dedicated writable handle (never read-modify-writing chunk rows) and gated by <code>recordRetrievals</code>; the GC folds it into each entry&#39;s <code>last_retrieved</code>. Identity is a reindex-surviving content hash (<code>entry_hash</code>) stored in a standalone <code>kb_entry_lifecycle</code> table (not a cascade child of <code>kb_chunks</code>), so age survives re-chunking. Indexing writes those rows for <strong>every</strong> source it sees, including files the content-hash guard skips as unchanged — a source indexed before the lifecycle table existed is backfilled from its stored mtime, so its real age is preserved rather than restarted (issue #398; without this the GC could only ever consider files edited after the feature shipped). <code>keepImportance</code> and <strong>pinned</strong> files (<code>memory/pinned/**</code>) are never aged out; evergreen Lane-1 (<code>MEMORY.md</code>/<code>USER.md</code>) is structurally excluded. Every move is CAS-guarded with a timestamped backup and drops <strong>no live session</strong> (memory-only, Part A). The read-path recorder is gated to the MCP layer via <code>GATEWAY_RECORD_RETRIEVALS</code>. Fields: <code>enabled</code> (default <code>true</code>; <code>false</code> ⇒ GC no-ops), <code>staleTtlDays</code> (default <code>90</code>), <code>keepImportance</code> (default <code>7</code>), <code>minRetrievalKeep</code> (default <code>1</code>), <code>supersession</code> (default <code>true</code>), <code>recordRetrievals</code> (default <code>true</code>), <code>maxInvalidationsPerRun</code> (default <code>50</code>) — a ceiling on how many entries ONE run may soft-invalidate, oldest-idle first, with the remainder resuming on later runs. Aging is wall-clock driven, so without it the first run after anything that widens the GC&#39;s visibility (such as the lifecycle backfill above) would relocate every already-expired entry in a single night. Restores are never capped. Per-agent override under <code>agents[].dreaming.staleness</code>. Injected by migration at <code>configVersion</code> <code>1.0.26</code>.</p><p><strong>Knowledge archive (<code>gateway.knowledge.archive</code>).</strong> Two-lane memory (planning-64). A per-agent SQLite/FTS5 index (<code>agents/&lt;id&gt;/kb.sqlite</code>, built on <code>node:sqlite</code> — zero new dependency) over the agent&#39;s <code>memory/*.md</code> notes plus the evergreen <code>MEMORY.md</code>/<code>USER.md</code>, so an agent can <strong>retrieve on demand</strong> what does not fit in the always-injected core. Injected by migration at <code>configVersion</code> <code>1.0.21</code>. Indexing is <strong>hash-guarded</strong> (unchanged files skipped), tags every chunk with <strong>fail-closed provenance</strong> (<code>owner</code>/<code>agent</code>/<code>untrusted</code>/<code>system</code>; unclassified ⇒ <code>untrusted</code>), and prunes only genuinely-absent files (a transient read error never drops a live source). The index is refreshed by a <strong>detached subprocess at agent-session spawn</strong>, so all synchronous SQLite work runs off the gateway event loop. Retrieval is exposed to the agent as two read-only MCP tools — <strong><code>memory_search</code></strong> (keyword/FTS5 over the archive → ranked snippets with file+line range, provenance, importance) and <strong><code>memory_get</code></strong> (bounded exact excerpt of a memory-scoped file by line range; path-traversal-guarded). The MCP layer runs under Bun, so those tools read <code>kb.sqlite</code> via <code>bun:sqlite</code>. Fields: <code>enabled</code> (default <code>true</code>; <code>false</code> ⇒ complete no-op, no DB created), <code>tokenizer</code> (FTS5 tokenizer, default <code>&quot;unicode61&quot;</code>; <code>&quot;trigram&quot;</code> for CJK/Thai), <code>chunkTokens</code> (default <code>400</code>), <code>chunkOverlap</code> (default <code>80</code>, clamped below <code>chunkTokens</code>). Per-agent override under <code>agents[].knowledge</code>. <strong>Core-shrink:</strong> when the archive is enabled and <code>MEMORY.md</code> is over its soft budget, compose injects a compact <strong>auto-generated section index</strong> (headings + a brief per top-level section) plus a pointer to <code>memory_search</code>, instead of the banner + truncated full text — so the bulk never enters the prompt. The index is derived deterministically at compose time (no LLM); the on-disk <code>MEMORY.md</code> is never modified, and its full content stays searchable via the archive. Falls back to the banner + full text when the archive is disabled or the file has no headings to index. <strong>Retrieval note:</strong> whenever the archive is enabled, a short platform-level <code>--- MEMORY RETRIEVAL ---</code> section is injected into every agent&#39;s system prompt (independent of the over-budget shrink path), so the <code>memory_search</code>/<code>memory_get</code> tools are discoverable at all times rather than only when the shrink pointer appears — a capability shared by all agents, injected once here instead of duplicated into each agent&#39;s <code>AGENTS.md</code>.</p><p><strong>Shared KB (<code>gateway.knowledge.shared</code>).</strong> A cross-agent knowledge base (planning-64 K3): a shared SQLite/FTS5 vault outside any single agent&#39;s workspace (<code>&lt;root&gt;/&lt;project&gt;/kb.sqlite</code>, default root <code>~/.claude-gateway/shared/kb</code>). The gateway has no built-in project concept, so sharing is keyed by an explicit <code>project</code> value — agents with the same <code>project</code> share one vault; <code>project</code> defaults to <code>&quot;global&quot;</code> (all agents share by default). Notes dropped under the vault&#39;s <code>notes/</code> dir are indexed (provenance <code>agent</code> — the owner&#39;s own trust domain) and reachable via <code>memory_search</code> with <code>corpus:&quot;shared&quot;</code> (the shared vault) or <code>corpus:&quot;all&quot;</code> (this agent&#39;s memory + shared, merged by relevance). Concurrent writers are safe without an in-process lock: note files are written temp-then-atomic-rename, and the shared index is guarded by <code>PRAGMA busy_timeout</code> across processes. The gateway passes the resolved vault dir to the MCP layer via <code>GATEWAY_SHARED_KB_DIR</code>. Fields: <code>enabled</code> (default <code>true</code>), <code>project</code> (default <code>&quot;global&quot;</code>; validated to one safe path segment), <code>root</code> (default <code>~/.claude-gateway/shared/kb</code>), <code>mode</code> (<code>propose</code>|<code>auto</code>, default <code>auto</code> — governs per-agent→shared promotion of durable dreamed facts), <code>graph</code> (default <code>false</code>). Per-agent override under <code>agents[].knowledge.shared</code>. <strong>On-demand read/write:</strong> the <strong><code>memory_shared_create</code></strong>/<strong><code>memory_shared_get</code></strong>/<strong><code>memory_shared_update</code></strong>/<strong><code>memory_shared_delete</code></strong> MCP tools let any agent create, read, update, and delete notes in the vault immediately (works in either <code>mode</code>, since these are explicit calls rather than automatic promotion). A note&#39;s identity is a freeform <code>name</code> the caller picks — there is no agent-id prefix and no per-agent ownership; any agent can create, edit, or delete any note. <code>memory_shared_create</code> fails on an exact-name collision (use <code>memory_shared_update</code> instead), and before writing also searches the shared KB for content-similar notes, returning them instead of creating unless called with <code>confirm:true</code> — this keeps agents from independently creating near-duplicate notes under different names; when <code>confirm:true</code> proceeds past a near-dup nudge, each related note found is linked into the new note&#39;s content as a <code>[[wikilink]]</code> rather than left as a disconnected duplicate. <code>memory_shared_update</code> fails if the name doesn&#39;t exist yet (use <code>memory_shared_create</code> instead), and replaces the note&#39;s full content; if the new content would drop 50%+ of the existing note&#39;s lines, it returns a warning instead of writing, requiring <code>confirm:true</code> to proceed — a guard against blindly clobbering a note instead of editing it. <code>memory_shared_get</code> returns a note&#39;s full current content (<code>memory_search</code> only returns short snippets). <strong>Nightly promotion shares this same freeform namespace</strong> (issues #386, #398): a promoted fact is named after its dream proposal&#39;s <code>topic</code> slug when the reviewer supplied one, else its <code>reason</code>, so the same recurring fact maps to the same note across dream nights and lands as an update, not a new file. A fallback name that reads as an <em>editing instruction</em> rather than the name of a fact (an imperative edit verb plus a positional/file anchor, e.g. &quot;insert after the cron section&quot;) is passed over, since no future occurrence of that fact could ever match such a name — the note is named from the fact itself instead, and the promotion is abandoned only when nothing nameable remains. Every declined promotion is logged with its reason — including the two that used to be silent: a merge the note-size cap refuses, and an unexpected write failure (which still never fails the local dream). A name that doesn&#39;t collide is still checked against a Node-native near-duplicate search (a <code>node:sqlite</code> port of the same OR-match FTS query the tools above use under Bun), but that query is <strong>recall only</strong>: an unattended merge additionally requires real token containment between the promoted fact and the candidate note. Below that bar the fact is written as its own note — two notes are a recoverable tidy-up, two unrelated facts fused into one are not. <code>[[wikilink]]</code>s to related notes clear a <strong>lower</strong> bar than merges, because a link is an additive claim while a merge destroys the distinction between the notes; they are attached whether the fact merges or lands as a new note, so a fact below the merge bar never becomes a disconnected graph node. Containment is measured against each candidate&#39;s full note body rather than the FTS chunk that matched — though against a <strong>capped seed</strong>: the query carries at most <code>SEED_TERM_BUDGET</code> significant tokens, so the bar reads as &quot;half of the fact&#39;s leading topic words are already in this note&quot;, not half of the entire fact. Retired <code>stale__*</code> notes are excluded as merge targets, and a recurrence under a retired name folds the retired body back into the live note and deletes the twin — on <strong>both</strong> the create and the update path, since either leaves a live file at that name and the GC&#39;s own restore refuses from then on. A retired note stays indexed and searchable by design, so a twin left beside a live note of the same name would answer every query twice, permanently. The twin is dropped only once the merged write actually lands; a write the size cap refuses leaves it in place and logs why. Folding also prevents two files sharing one <code>entry_hash</code>. All four tools trigger an immediate reindex on write/delete, needing <code>GATEWAY_NODE_EXEC_PATH</code> (the gateway&#39;s own Node <code>execPath</code>, forwarded alongside <code>GATEWAY_SHARED_KB_DIR</code>) since the Bun MCP process cannot itself run the <code>node:sqlite</code>-based reindex CLI.</p><p><strong>Graph layer (<code>gateway.knowledge.shared.graph</code>).</strong> Opt-in memory-wiki-style compile (planning-64 K5) over the shared vault, run after each shared reindex (off the event loop). Deterministic — no LLM. Reads note YAML frontmatter (<code>title</code>, <code>claims[]</code> with <code>id</code>/<code>text</code>/<code>status</code>/<code>confidence</code>, page <code>confidence</code>, <code>updatedAt</code>) and body <code>[[wiki-links]]</code>, and writes <code>&lt;vault&gt;/reports/</code>: <code>relationship-graph.md</code> (every link edge), <code>backlinks.md</code> (reverse edges), <code>contradictions.md</code> (claims sharing an <code>id</code> whose text/status diverges), <code>stale-pages.md</code> (≥ 90 days old), <code>low-confidence.md</code> (confidence &lt; 0.5). The generated reports dir is never scanned as source.</p><h2 id="get-apiv1agentsagentidfilesfilename" tabindex="-1">GET /api/v1/agents/:agentId/files/:filename <a class="header-anchor" href="#get-apiv1agentsagentidfilesfilename" aria-label="Permalink to &quot;GET /api/v1/agents/:agentId/files/:filename {#get-apiv1agentsagentidfilesfilename}&quot;">​</a></h2><p>Read a workspace file. Returns empty <code>content</code> if the file does not exist yet (not a 404).</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;X-Api-Key: my-secret-key-123&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  http://localhost:10850/api/v1/agents/alfred/files/SOUL.md</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> jq</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;filename&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SOUL.md&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;content&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;# Soul</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">\\n\\n</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Alfred is warm, helpful, and precise.&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><hr><h2 id="put-apiv1agentsagentidfilesfilename" tabindex="-1">PUT /api/v1/agents/:agentId/files/:filename <a class="header-anchor" href="#put-apiv1agentsagentidfilesfilename" aria-label="Permalink to &quot;PUT /api/v1/agents/:agentId/files/:filename {#put-apiv1agentsagentidfilesfilename}&quot;">​</a></h2><p>Write a workspace file. Requires write access to the agent. Max 1MB.</p><p><strong>Request body:</strong></p><table tabindex="0"><thead><tr><th>Field</th><th>Required</th><th>Description</th></tr></thead><tbody><tr><td><code>content</code></td><td>Yes</td><td>Full file content as a string</td></tr></tbody></table><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">curl</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -X</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> PUT</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;X-Api-Key: admin-key-456&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -H</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &quot;Content-Type: application/json&quot;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  -d</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;{&quot;content&quot;: &quot;# Soul\\n\\nAlfred is warm, helpful, and precise.&quot;}&#39;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> \\</span></span>
<span class="line"><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">  http://localhost:10850/api/v1/agents/alfred/files/SOUL.md</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> jq</span></span></code></pre></div><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{ </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;filename&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;SOUL.md&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">&quot;message&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;File saved. CLAUDE.md will auto-reload.&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> }</span></span></code></pre></div><p><strong>Error responses:</strong></p><table tabindex="0"><thead><tr><th>Status</th><th>When</th></tr></thead><tbody><tr><td>400</td><td>Filename not in allowed list, invalid format, or content not a string</td></tr><tr><td>400</td><td>Content exceeds 1MB</td></tr><tr><td>403</td><td>Key has no write access to agent</td></tr><tr><td>404</td><td>Agent not found</td></tr></tbody></table><hr>`,28)])])}const u=t(s,[["render",n]]);export{g as __pageData,u as default};
