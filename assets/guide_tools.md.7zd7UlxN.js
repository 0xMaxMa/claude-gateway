import{_ as a,o as t,c as i,a2 as o}from"./chunks/framework.D1JEzK2q.js";const p=JSON.parse('{"title":"Tools and skills","description":"","frontmatter":{},"headers":[],"relativePath":"guide/tools.md","filePath":"guide/tools.md"}'),s={name:"guide/tools.md"};function n(r,e,l,d,c,h){return t(),i("div",null,[...e[0]||(e[0]=[o(`<h1 id="tools-and-skills" tabindex="-1">Tools and skills <a class="header-anchor" href="#tools-and-skills" aria-label="Permalink to &quot;Tools and skills&quot;">​</a></h1><p>MCP tools let an agent act through gateway integrations. Skills are Markdown instructions that teach an agent how to carry out a workflow with those tools.</p><h2 id="verify-tools-are-available" tabindex="-1">Verify tools are available <a class="header-anchor" href="#verify-tools-are-available" aria-label="Permalink to &quot;Verify tools are available&quot;">​</a></h2><p>Bun runs <code>mcp/server.ts</code>; its dependencies must be installed. After installation, ask an agent to list its available tools and try one read-only operation, such as listing cron jobs. Check the result rather than treating an announced intention as execution.</p><p>If tools are unavailable, check <code>bun --version</code>, MCP dependency installation, and the agent logs. In a source checkout, <code>make mcp-install</code> installs the MCP dependencies.</p><h2 id="add-a-skill" tabindex="-1">Add a skill <a class="header-anchor" href="#add-a-skill" aria-label="Permalink to &quot;Add a skill&quot;">​</a></h2><p>Create <code>skills/review-notes/SKILL.md</code> inside the agent workspace:</p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">---</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">name</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">review-notes</span></span>
<span class="line"><span style="--shiki-light:#22863A;--shiki-dark:#85E89D;">description</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">Review project notes and identify unanswered questions.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">---</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Read the relevant notes before summarizing.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Separate confirmed decisions from unanswered questions.</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">Link each finding to its source file.</span></span></code></pre></div><p>Skills hot-reload. Ask the agent to review a small note and verify that its answer cites the file. The discovery locations and precedence are described below.</p><h2 id="automatic-skill-learning" tabindex="-1">Automatic skill learning <a class="header-anchor" href="#automatic-skill-learning" aria-label="Permalink to &quot;Automatic skill learning&quot;">​</a></h2><p><code>gateway.skillLearning</code> controls background review of substantive work. It can create or update automatically learned skills, with daily and total limits and an audit in <code>SKILLS_LEARNED.md</code>. The provenance guard preserves human-authored skills. Review the audit when evaluating what the agent learned.</p><p>Connector availability and agent tool permissions also affect what a session can do. See <a href="./../reference/configuration.html">configuration</a> and the <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/mcp/server.ts" target="_blank" rel="noreferrer">MCP server implementation</a>.</p><h2 id="skill-discovery-and-precedence" tabindex="-1">Skill discovery and precedence <a class="header-anchor" href="#skill-discovery-and-precedence" aria-label="Permalink to &quot;Skill discovery and precedence&quot;">​</a></h2><p>The gateway discovers a skill as a directory containing <code>SKILL.md</code>. Hidden directories and <code>node_modules</code> are skipped. Keep supporting scripts and examples alongside the instruction file, and refer to them from the skill rather than putting a large manual in its description.</p><table tabindex="0"><thead><tr><th>Location</th><th>Registry name</th><th>Intended use</th></tr></thead><tbody><tr><td><code>&lt;workspace&gt;/skills/&lt;name&gt;/SKILL.md</code></td><td><code>&lt;name&gt;</code></td><td>Workflows specific to one agent</td></tr><tr><td><code>mcp/tools/&lt;module&gt;/skills/&lt;name&gt;/SKILL.md</code></td><td><code>&lt;module&gt;:&lt;name&gt;</code></td><td>Instructions shipped with an MCP module</td></tr><tr><td><code>~/.claude-gateway/shared-skills/&lt;name&gt;/SKILL.md</code></td><td><code>&lt;name&gt;</code></td><td>Reusable instructions shared across agents</td></tr></tbody></table><p>Loading proceeds from shared to module to workspace, so a workspace entry overrides an existing registry key. Module prefixes normally prevent collisions with plain workspace/shared names. The generated menu lists user-invocable skills grouped by source. Invoke a workspace skill with <code>/review-notes path/to/notes.md</code>, or a module skill with <code>/&lt;module&gt;:&lt;name&gt; [arguments]</code>.</p><p>Shared and module instructions are also synchronized into <code>~/.claude/skills/</code> for Claude&#39;s personal skill discovery. <code>.shared</code> and <code>.module</code> marker files identify managed copies; removed source skills cause their marked copies to be cleaned up. Edit the source under <code>shared-skills</code> or the module, because later synchronization can replace edits to a managed copy.</p><p>Source: <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/skills/loader.ts" target="_blank" rel="noreferrer">skill loader</a>, <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/skills/sync.ts" target="_blank" rel="noreferrer">synchronization</a>, and <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/index.ts" target="_blank" rel="noreferrer">startup/watch wiring</a>.</p><h2 id="learning-controls-and-review-flow" tabindex="-1">Learning controls and review flow <a class="header-anchor" href="#learning-controls-and-review-flow" aria-label="Permalink to &quot;Learning controls and review flow&quot;">​</a></h2><p>Learning is enabled in <code>auto</code> mode by default. A completed session qualifies for review when it reaches the tool-call threshold, fires recovery, or contains a detected user correction, subject to the daily review budget. A qualifying session does not guarantee a useful skill will be written: the reviewer and writer still evaluate the proposal.</p><table tabindex="0"><thead><tr><th>Setting</th><th>Built-in default</th><th>Effect</th></tr></thead><tbody><tr><td><code>minToolCalls</code></td><td><code>5</code></td><td>Tool-call trigger threshold</td></tr><tr><td><code>maxReviewsPerDay</code></td><td><code>20</code></td><td>Daily background review ceiling</td></tr><tr><td><code>reviewModel</code></td><td><code>claude-haiku-4-5-20251001</code></td><td>Background review model</td></tr><tr><td><code>maxAutoSkills</code></td><td><code>50</code></td><td>Learned-skill capacity</td></tr><tr><td><code>maxAgeDays</code> / <code>minUsesToKeep</code></td><td><code>30</code> / <code>2</code></td><td>Curator age/use policy</td></tr><tr><td><code>pruneHour</code></td><td><code>3</code></td><td>Daily curator hour</td></tr><tr><td><code>notify</code></td><td><code>true</code></td><td>Learning notifications</td></tr></tbody></table><p>Per-agent <code>skillLearning</code> values override <code>gateway.skillLearning</code>, then built-in defaults apply. The pruning timezone resolves through agent override, gateway skill-learning override, <code>gateway.timezone</code>, then UTC; invalid timezone values fall through to the next level.</p><p>Choose <code>mode: &quot;propose&quot;</code> to queue candidate instructions in <code>skills/.pending/</code> for review before making them live. In <code>auto</code> mode, inspect the live skill and <code>SKILLS_LEARNED.md</code> after a learning event. The writer marks learned skills <code>origin: auto</code> and refuses to overwrite a human-authored target. Missing provenance is treated as human-authored. Curator policy applies to automatically learned skills, not a blanket deletion of all old instructions.</p><p>The menu shortens auto-learned descriptions to 80 characters to bound recurring context cost; the full skill body remains available when invoked.</p><p>Source: <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/agent/skill-learning/config.ts" target="_blank" rel="noreferrer">learning defaults</a>, <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/agent/skill-learning/trigger.ts" target="_blank" rel="noreferrer">trigger gates</a>, and <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/agent/skill-learning/writer.ts" target="_blank" rel="noreferrer">provenance-aware writer</a>.</p><h2 id="diagnose-a-missing-skill-or-tool" tabindex="-1">Diagnose a missing skill or tool <a class="header-anchor" href="#diagnose-a-missing-skill-or-tool" aria-label="Permalink to &quot;Diagnose a missing skill or tool&quot;">​</a></h2><p>Check the exact directory shape, valid YAML frontmatter, and skill name first. An unreadable file can be skipped; a hidden <code>.pending</code> candidate is intentionally not a live skill. Then check the generated menu and any same-name workspace override. Missing binary requirements produce warnings, so discovery alone does not establish that a workflow can run.</p><p>A skill supplies instructions, while an MCP tool supplies executable capability. Installing a skill does not install its required binary, authenticate a connector, or broaden an agent&#39;s tool permissions. Test the smallest read-only tool involved in the workflow, then invoke the skill on a small example.</p><h2 id="native-claude-code-skills-in-task-execution" tabindex="-1">Native Claude Code skills in task execution <a class="header-anchor" href="#native-claude-code-skills-in-task-execution" aria-label="Permalink to &quot;Native Claude Code skills in task execution&quot;">​</a></h2><p>The orchestration catalog combines gateway-discovered Markdown skills with skills reported by the actual Claude Code worker runtime, including bundled and enabled user/plugin skills. A native skill such as <code>code-review</code> does not need a gateway <code>SKILL.md</code>. Use the exact discovered name rather than guessing it from a directory.</p><p>Native discovery performs a Claude Code initialization probe without a user prompt, model call, MCP server, or hooks. The worker rechecks availability in its selected runtime before executing a native skill. <code>CLI_SKILL_UNAVAILABLE</code> means it was not available there; the gateway does not silently retry on a different host/container runtime.</p><p>For a gateway Markdown skill task, the driver builds a task-specific Claude plugin containing the pinned skill body and supporting resources, then invokes <code>orchestration-task:&lt;name&gt;</code>. Plugin requirements that cannot be satisfied are rejected. Symlinks are not copied as skill resources; container transfer also imposes a 10 MiB resource limit.</p><p>Host workers use Claude&#39;s default native tool set and can retain host runtime customization. Restricted non-host profiles explicitly select native tools, require the generated MCP configuration, disable hooks, and suppress unrelated enabled plugins. Consequently a skill working in an interactive host shell is not proof it will work inside a restricted or container task profile. Check the actual discovered inventory and task error before installing or changing anything.</p><p>Source: <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/orchestration/cli-skills.ts" target="_blank" rel="noreferrer">native skill discovery</a>, <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/orchestration/tasks/driver.ts" target="_blank" rel="noreferrer">skill task preparation</a>, and <a href="https://github.com/0xMaxMa/claude-gateway/blob/b917843/src/session/runtime-profile.ts" target="_blank" rel="noreferrer">runtime profile arguments</a>.</p>`,34)])])}const k=a(s,[["render",n]]);export{p as __pageData,k as default};
