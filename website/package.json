{
  "name": "claude-gateway-website",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vitepress dev --host 127.0.0.1",
    "build": "vitepress build",
    "preview": "vitepress preview --host 127.0.0.1",
    "check": "npm run build && node scripts/check-links.mjs"
  },
  "devDependencies": {
    "vitepress": "1.6.4"
  },
  "overrides": {
    "vite": "6.4.3"
  }
}
